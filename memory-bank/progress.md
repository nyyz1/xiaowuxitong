# Progress

## 2026-05-09

- User asked for a more convenient default or one-click way to continue Codex development across different work locations and different logged-in GitHub accounts.
- Re-read the required memory-bank files and treated the work as a Step 7 collaboration and handoff convenience refinement.
- Changes made:
  - added `setup-dev-workstation.cmd` plus `scripts/setup-dev-workstation.ps1` for first-time setup on another development computer, including Git/Node checks, optional winget installation, Git safe-directory setup, `.env.local` preparation, dependency installation, and Prisma client generation
  - added `start-work.cmd` plus `scripts/start-work.ps1` for pre-Codex work sync from GitHub, with a guard that stops when local unsaved changes exist
  - added `save-work.cmd` plus `scripts/save-work.ps1` for end-of-session typecheck, lint, commit, rebase, and push
  - added `docs/codex-multi-workstation.md` and linked it from `README.md`
- Verification completed so far:
  - PowerShell parser checks passed for the three new `.ps1` scripts
  - `npm.cmd run typecheck`
  - `npm.cmd run lint`
- Next step:
  - commit and push the new convenience workflow

- User provided the new GitHub repository `https://github.com/nyyz1/xiaowuxitong.git` and asked how to push the local project for remote collaboration.
- Re-read the required memory-bank files and treated the work as a Step 7 collaboration and handoff preparation task.
- Findings:
  - Git was not installed or available on PATH on the current workstation.
  - The project directory had not yet been initialized as a Git repository.
  - `.gitignore` already protected `.env.local`, `.next`, `node_modules`, and `local/`, but it also accidentally ignored `.env.example` and did not ignore local `artifacts` or `logs`.
- Changes made:
  - installed Git for Windows through `winget`
  - initialized the repository in `D:\xiaowuxitong`
  - updated `.gitignore` so `.env.example` is committed while `.env.local`, generated build output, dependencies, local tunnel material, artifacts, logs, and TypeScript build info stay out of Git
- Verification so far:
  - checked staged files and confirmed `.env.local`, `node_modules`, `.next`, `local`, `logs`, and `artifacts` are not staged
- Next step:
  - create the first local commit, add the GitHub `origin`, and push `main`

- User requested implementation of the mobile-first task-entry UI plan inspired by Open Design, focused on faster student lookup and routine inspection entry without importing the external Open Design runtime.
- Re-read the required memory-bank files and treated the work as a Step 7 interface stabilization and handoff-experience refinement.
- Changes made:
  - added `/dashboard/quick/students` as a focused active-student quick-search route that reuses existing people permissions, grade scope, active-cohort filtering, and profile-field definitions
  - added `/dashboard/quick/inspection` as a focused routine-inspection quick-entry route that reuses existing inspection recorder permissions, grade-scope target-type coercion, active item lookup, and student/teacher scope selectors
  - kept the left sidebar hierarchy unchanged while adding task cards on the dashboard overview and compact quick links in the top bar
  - extended `createInspectionRecord` with a safe `returnMode=quick` redirect mode that only returns to `/dashboard/quick/inspection` with whitelisted target type, date, and item parameters
  - added lightweight query helpers for student quick search and inspection quick entry so the quick pages do not load full maintenance-page data
- Verification completed so far:
  - `npm.cmd run lint -- src/app/dashboard/quick/students/page.tsx src/app/dashboard/quick/inspection/page.tsx src/components/shell/dashboard-shell.tsx src/app/dashboard/page.tsx src/modules/people/queries.ts src/modules/inspection/queries.ts src/modules/inspection/actions.ts`
  - `npm.cmd run typecheck`
  - `npm.cmd run build`
  - temporary `next start` on `127.0.0.1:3001` plus scripted credential checks for `admin`, `data.manager`, `inspector`, `grade11.manager1`, and `leader1`, confirming the quick-search and quick-entry route boundaries
- Verification note:
  - Browser plugin visual smoke was attempted twice but timed out while connecting to the in-app browser, so mobile screenshot validation was not completed in this pass.
  - The already running port `3000` service still served the older production process during verification; new quick routes were verified against a temporary `3001` process using the freshly built app.
- Next step:
  - restart the live school-pilot process after this build so office users on port `3000` receive the new quick routes instead of the older build

- User asked to expose the local school-pilot app through the Tencent Cloud server because same-Wi-Fi LAN access is blocked by the school network.
- Re-read the required memory-bank files and treated the work as a Step 7 deployment and handoff hardening slice.
- Findings:
  - the local Next.js app was already healthy on `127.0.0.1:3000`
  - the Tencent Cloud server `119.45.252.190` was reachable on `22`, `80`, and `443`
  - the server already ran `Caddy` on `80/443`, `frps`, and another app on server port `3000`
  - an SSH reverse tunnel from the workstation to server-local `127.0.0.1:63000` was verified working
  - direct IP plus extra-port exposure is currently blocked by Tencent Cloud public ingress policy outside the VM, even when the server is listening locally
- Changes made:
  - added `scripts/start-school-public-tunnel.ps1` and `start-school-public-pilot.cmd` for a public reverse-tunnel launch flow
  - updated `scripts/start-school-pilot.ps1` so it can accept an explicit external base URL instead of only a LAN IP based URL
  - updated `.gitignore` to keep local tunnel material under `local/` out of git
  - during relay testing, updated `.env.local` so `NEXTAUTH_URL` can point at a public relay host instead of only the LAN IP
  - added a dedicated `Caddy` site on the Tencent Cloud server that proxies to the reverse-tunnel port `127.0.0.1:63000`
- Verification:
  - `npm.cmd run typecheck`
  - local `http://127.0.0.1:3000/login` returned `200`
  - server-local `curl -I http://127.0.0.1:63000/login` returned `200` through the reverse tunnel
  - confirmed the remaining blocker is Tencent Cloud public ingress policy for the chosen extra IP port, not app health or tunnel health
- Risks or blockers:
  - `http://119.45.252.190` already serves another site, so switching the bare IP root to the school app would replace that existing entry
  - direct IP plus extra-port access requires opening that port in the Tencent Cloud security group; this cannot be solved from inside the VM alone
- Next step:
  - open one dedicated public TCP port in the Tencent Cloud security group, then bind that public port to the existing reverse-tunnel target and revalidate `IP:port/login`
- Follow-up:
  - after port `62000` was opened in the Tencent Cloud security group, a visible long-running `plink` reverse-SSH window was used to keep `62000 -> 127.0.0.1:3000` active over the already-allowed SSH `22` path
  - verified `http://119.45.252.190:62000/login` now returns `200 OK`, while preserving the existing root IP site on port `80`
  - refined the public launcher into a one-click flow that prepares `NEXTAUTH_URL`, starts or reuses the local app window, and starts or reuses the visible `plink` tunnel window for the public `62000` path

- User asked to fix the remaining Chinese-document mojibake problem.
- Re-read the required memory-bank files and treated the work as a Step 7 documentation and handoff reliability repair.
- Findings:
  - the main user-facing markdown docs in `README.md`, `docs/`, and `tasks/` were still valid UTF-8 Chinese in file content
  - the earlier garbled output was mostly caused by Windows PowerShell default-reading those files as the local ANSI code page instead of UTF-8
  - `SCHOOL_TRANSFER_README.txt` was the only short handoff file whose content had already been written in a garbled state and needed a real text rewrite
- Changes made:
  - normalized the main Chinese-facing markdown and text handoff files to UTF-8 with BOM so Windows tools are more likely to display them correctly by default
  - rewrote `SCHOOL_TRANSFER_README.txt` into clean Chinese text
  - preserved document meaning instead of mass-rewriting already-correct markdown content
- Verification:
  - reopened `README.md`, `docs/school-transfer-one-click.md`, `docs/deployment-and-smoke-test.md`, `tasks/lessons.md`, and `SCHOOL_TRANSFER_README.txt` with explicit `UTF8` decoding and confirmed the Chinese content is readable
  - confirmed the previously garbled markdown content in `README.md` and `docs/` was a display-layer issue rather than widespread file-content corruption
- Risk or blocker:
  - Windows PowerShell 5.1 can still display a plain `.txt` file incorrectly when it guesses the encoding instead of reading it as UTF-8; the file content itself is now corrected

- User requested a `neat-freak` cleanup after migration acceptance and repair.
- Re-read the required memory-bank files, the named local neat-freak skill file, root markdown, docs, and task notes.
- Changes made:
  - added migrated-workstation acceptance addenda to `README.md`, `docs/school-transfer-one-click.md`, `docs/deployment-and-smoke-test.md`, and `docs/postgresql-install-and-acceptance-runbook.md`
  - clarified that the current migrated workstation is valid with PostgreSQL under `C:\Program Files\PostgreSQL\17\data`, while the older D-drive PostgreSQL layout remains the earlier pilot workstation shape
  - updated `src/modules/data-management/backup.ts` so deletion-before-backup can find `pg_dump.exe` in both `D:\PostgreSQL\17\bin` and `C:\Program Files\PostgreSQL\17\bin` when `PG_DUMP_PATH` is not set
  - updated `memory-bank/architecture.md` so data-management backup flow and workstation notes match the current C-drive PostgreSQL installation
  - added a migration acceptance lesson to `tasks/lessons.md`
  - added a completed migrated-workstation acceptance cleanup section to `tasks/todo.md`
- Verification:
  - `npm.cmd run lint -- src/modules/data-management/backup.ts`
  - `npm.cmd run typecheck`
  - `npm.cmd run build`
  - live `pg_dump` smoke against `school_affairs` using the current C-drive PostgreSQL tool path
- Risk or blocker:
  - several older Chinese markdown sections still contain mojibake from earlier encoding damage; this pass added clear current notes rather than rewriting all historical handoff prose.

- User asked to complete migration acceptance on the newly moved workstation and then approved fixing the discovered migration issues directly.
- Re-read the required memory-bank files and treated the work as a Step 7 migration acceptance and workstation recovery task.
- Findings:
  - `node -v` works on the migrated workstation, but direct `npm` in PowerShell is blocked by execution policy; `npm.cmd` works and should be used by scripts or manual commands.
  - PostgreSQL command-line tools are installed under `C:\Program Files\PostgreSQL\17\bin`, but `psql` is not on the current shell PATH.
  - PostgreSQL was installed by the school-workstation setup path under `C:\Program Files\PostgreSQL\17\data`, with service `postgresql-x64-17`, rather than under the earlier pilot workstation's `D:\PostgreSQL\data` path.
  - `.env.local` expected `school_admin / school_password`, but the freshly installed PostgreSQL instance did not yet have a matching application database login, so direct `school_admin` connection failed.
  - The PostgreSQL superuser password was confirmed as `postgres`.
- Changes made:
  - used the confirmed `postgres` superuser account to create the `school_affairs` database and create or reset the `school_admin` login to match `.env.local`
  - granted and owned the `public` schema to `school_admin`
  - pushed the current Prisma schema into the new local `school_affairs` database
  - seeded the deterministic trial/demo dataset so the migrated workstation has the approved login accounts and enough data for smoke testing
  - started the production Next.js service on `0.0.0.0:3000` through the Next CLI after confirming the standard production start command reaches `Ready`
- Verification:
  - `npm.cmd run typecheck`
  - `npm.cmd run lint`
  - `npm.cmd run build`
  - `DATABASE_URL=postgresql://school_admin:school_password@localhost:5432/school_affairs?schema=public npm.cmd run db:validate`
  - `DATABASE_URL=postgresql://school_admin:school_password@localhost:5432/school_affairs?schema=public npm.cmd run db:generate`
  - `DATABASE_URL=postgresql://school_admin:school_password@localhost:5432/school_affairs?schema=public npm.cmd run db:push`
  - `DATABASE_URL=postgresql://school_admin:school_password@localhost:5432/school_affairs?schema=public npm.cmd run db:seed:demo`
  - `psql` connection as `school_admin` to `school_affairs` succeeds with the application password
  - `http://127.0.0.1:3000/login` and `http://10.1.67.159:3000/login` return `200 OK`
  - scripted `admin / ChangeMe123!` login plus the app's browser-session cookie could access `/dashboard`, `/dashboard/structure`, `/dashboard/users`, `/dashboard/people`, `/dashboard/archive/students`, `/dashboard/inspection`, `/dashboard/exports`, and `/dashboard/data-management`, all returning `200 OK`
- Risks or blockers:
  - operator-facing Chinese markdown still displays mojibake in the current terminal output and should be cleaned before handing the docs to non-technical school users
  - the migrated workstation currently uses PostgreSQL under `C:\Program Files\PostgreSQL\17\data`; moving the data directory to `D:` would be a separate Windows/PostgreSQL administration task
  - the current `NEXTAUTH_SECRET` and demo/default passwords remain trial defaults and should be changed before real production data entry
  - the current service process is running now, but the normal one-click launcher should still be used for day-to-day startup so `logs/current-school-pilot-url.txt` stays refreshed after network changes
- Next step:
  - clean the migrated workstation handoff docs or update the setup script/docs to explicitly handle the current PostgreSQL install location, `npm.cmd` PowerShell behavior, and PATH notes.

## 2026-05-08

- User asked whether the school-computer prerequisites can also be automated: check/install Node.js LTS and PostgreSQL, then create the `school_admin` user and `school_affairs` database automatically.
- Re-read the required memory-bank files and treated the work as a Step 7 school-workstation deployment convenience refinement.
- Changes made:
  - extended `scripts/setup-school-workstation.ps1` so it checks for Node.js/npm and attempts `winget install OpenJS.NodeJS.LTS` when missing
  - extended the setup script so it checks for PostgreSQL `psql.exe` across common `D:\PostgreSQL` and `Program Files\PostgreSQL` paths and attempts `winget install PostgreSQL.PostgreSQL.17` when missing
  - added PostgreSQL Windows service startup detection for `postgresql*` services
  - added a secure prompt for the PostgreSQL administrator password, plus automatic creation or update of the application role and database
  - kept the default application database connection as `postgresql://school_admin:school_password@localhost:5432/school_affairs?schema=public`, with parameters available for custom database name, user, password, admin user, and admin password
  - added `-SkipPrerequisiteInstall` and `-SkipDatabaseBootstrap` escape hatches for school environments where IT already manages software and databases manually
  - updated `setup-school-workstation.cmd`, `docs/school-transfer-one-click.md`, `README.md`, `tasks/todo.md`, and `memory-bank/architecture.md`
- Verification:
  - PowerShell parser syntax check passed for `scripts/setup-school-workstation.ps1` and `scripts/package-for-school.ps1`
  - `npm run typecheck`
  - `npm run lint`
  - `npm run package:school` regenerated `artifacts\school-transfer\xiaowuxitong-school-transfer-20260508-202341.zip`
  - inspected the generated zip and confirmed it contains the updated setup script and transfer docs while still excluding `.env.local`, dependency/build/runtime directories, dump files, and log files
  - the school-computer setup script was not executed end-to-end on this home workstation because it may install software, prompt for PostgreSQL admin credentials, create or update database roles, sync schema, seed data, build, and start the pilot service

- User asked for a more convenient one-click way to move only the project to a school office computer without carrying the current home database data.
- Re-read the required memory-bank files and treated the work as a Step 7 deployment and handoff convenience task.
- Changes made:
  - added `scripts/package-for-school.ps1` and root `package-for-school.cmd` to create a timestamped project-transfer zip under `artifacts\school-transfer`
  - the transfer packager excludes `.env.local`, `node_modules`, `.next`, `.tmp`, `logs`, `outputs`, `artifacts`, `.git`, dump files, zip files, and log files so local data and build/runtime state are not carried
  - added `scripts/setup-school-workstation.ps1` and root `setup-school-workstation.cmd` for first-run setup on the school computer
  - the setup script prepares `.env.local`, generates a fresh `NEXTAUTH_SECRET` when needed, installs dependencies, runs Prisma generation and schema sync, optionally writes demo accounts/data, builds, and starts the normal pilot launcher
  - added `npm run package:school` and `npm run setup:school`
  - added `docs/school-transfer-one-click.md` and updated `README.md` with the project-only transfer flow
  - updated `tasks/todo.md` and `memory-bank/architecture.md` with the new transfer boundary
- Verification:
  - PowerShell parser syntax check passed for `scripts/package-for-school.ps1` and `scripts/setup-school-workstation.ps1`
  - `npm run package:school` created `artifacts\school-transfer\xiaowuxitong-school-transfer-20260508-201424.zip`
  - inspected the generated zip and confirmed it does not contain `.env.local`, `node_modules`, `.next`, `.git`, `logs`, `outputs`, `artifacts`, dump files, or log files
  - `npm run typecheck`
  - `npm run lint`
  - the school-computer setup script was not executed end-to-end on this home workstation because it would sync PostgreSQL, seed data, build, and start the pilot service

- User requested a `neat-freak` cleanup pass after the row-level mistaken-entry correction and deletion feature.
- Re-read the required memory-bank files, root markdown files, docs, and task notes.
- Changes made:
  - updated `README.md` so the current-state summary says people records and inspection records support mistaken-entry modification and deletion
  - updated `docs/pilot-accounts-and-usage-guide.md` so data managers, inspection staff, page descriptions, and role boundaries mention mistaken-entry deletion
  - updated `docs/postgresql-install-and-acceptance-runbook.md`, `docs/security-and-deployment-plan.md`, and `docs/approval-brief.md` so acceptance, security, and approval notes include correction/deletion behavior and audit coverage
  - added a completed Step 4/5 correction-and-deletion section to `tasks/todo.md`
  - merged the conflicting 2026-05-08 sidebar lessons into one final rule that keeps production sidebar hierarchy stable unless the visible hierarchy is explicitly requested and confirmed
  - updated `memory-bank/architecture.md` to record that operator-facing docs now describe row-level correction/deletion boundaries
- Verification:
  - searched project markdown for people, inspection, deletion, audit, and role references before editing
  - no code was changed in this cleanup pass

- User requested convenient correction and deletion for daily people-profile entry and routine-inspection entry mistakes.
- Re-read the required memory-bank files and treated the work as a Step 4 people-records correction refinement plus a Step 5 routine-inspection correction refinement.
- Changes made:
  - added row-level delete server actions for teachers, students, and inspection records
  - kept people deletion behind existing people-editor access, and kept inspection deletion behind existing inspection-recorder access plus grade-scope checks
  - blocked teacher hard deletion when the teacher already has inspection records, so teacher-quantification history is not silently detached
  - wrote delete actions into `AuditLog` with the deleted row snapshot needed for later review
  - added delete buttons beside the existing edit/status controls on active teacher, active student, archived student, and inspection record detail panels
  - added a reusable confirmation prompt option to the shared submit button so destructive row actions require browser confirmation before submission
  - updated `memory-bank/prd.md`, `memory-bank/implementation-plan.md`, and `memory-bank/architecture.md` to record the new correction/delete scope
- Verification:
  - `npm run lint -- src/components/form/submit-button.tsx src/lib/validation/people.ts src/lib/validation/inspection.ts src/modules/people/actions.ts src/modules/people/page.tsx src/modules/alumni-archive/page.tsx src/modules/inspection/actions.ts src/modules/inspection/page.tsx`
  - `npm run typecheck`
  - `npm run build`
- Verification note:
  - this pass did not execute a real delete against the live PostgreSQL database; validation covered compile-time behavior and production build shape

- User clarified that the visible `学生档案 / 教师档案` submenu entries under the sidebar `师生档案` item should be removed.
- Re-read the required memory-bank files and treated the work as a Step 4 people-records navigation cleanup.
- Changes made:
  - restored the sidebar `师生档案` item to a single direct link to `/dashboard/people`
  - removed the expanded `学生档案` and `教师档案` sidebar children that were shown in the left navigation
  - kept the internal `/dashboard/people?view=students|teachers` handling and role boundary code in place so existing focused people-page behavior remains stable
- Verification:
  - `npm run lint -- src/components/shell/dashboard-shell.tsx`
  - `npm run typecheck`
  - `npm run build`

- User asked for `师生档案` to match the student/teacher split pattern already used by routine inspection and statistics export, with second-level entries for student records and teacher records.
- Re-read the required memory-bank files and treated the work as a Step 4 people-records usability refinement with Step 7 navigation-stabilization impact.
- Changes made:
  - changed the dashboard sidebar so `师生档案` is now a parent menu with `学生档案` and `教师档案` second-level entries; grade-scoped managers only receive the student entry because they cannot view school-wide teacher data
  - added `view=students` and `view=teachers` handling for `/dashboard/people`, defaulting to student records and forcing grade-scoped roles back to the student view
  - changed the people page so each entry focuses on the selected record type instead of always showing teacher and student sections together
  - kept active-student and teacher filters, templates, imports, exports, save actions, status actions, and profile-category redirects on the selected student or teacher entry after each operation
  - updated the people-page profile-category configuration to show the category manager for the selected record type only
- Verification:
  - `npm run lint -- src/components/shell/dashboard-shell.tsx src/app/dashboard/people/page.tsx src/modules/people/page.tsx src/modules/people/actions.ts`
  - `npm run typecheck`
  - `npm run build`
- Verification note:
  - this pass changed navigation and page focus only; no database schema, PostgreSQL data, import column definitions, export file builders, or permission rules were changed

- User approved applying the recommended `校务档案馆` visual direction.
- Re-read the required memory-bank files and treated the work as a Step 7 interface stabilization and handover-experience refinement.
- Design direction:
  - translated the Open Design / Atelier Zero mood into an operations-safe backend style rather than using a marketing deck style directly
  - used warm paper surfaces, ink-green navigation, subtle grid texture, fine divider lines, and coral signal dots while preserving data-entry density and Ant Design form/table behavior
- Changes made:
  - revised `src/app/globals.css` with the `校务档案馆` color system, paper panels, archive mark, coral dot accents, sidebar menu states, table headers, and shared dashboard surface rules
  - revised `src/app/layout.tsx` Ant Design tokens to match the paper/ink/coral style
  - revised `src/components/shell/dashboard-shell.tsx` with a stronger school archive brand mark, ink-green sidebar, route status header, role display, and a compact RBAC note
  - revised `src/components/auth/login-form.tsx` into a formal archive-style login entry with a paper cover panel and cleaner credential card
  - refined `src/app/dashboard/page.tsx` into a `校务总台` overview using paper panels, numbered module entry tiles, coral accents, and clearer module responsibility sections
- Verification:
  - `npm run lint -- src/app/globals.css src/app/layout.tsx src/components/shell/dashboard-shell.tsx src/components/auth/login-form.tsx src/app/dashboard/page.tsx` completed with only the expected CSS ignored-file warning
  - `npm run typecheck`
  - `npm run build`
  - `npm run lint`
  - started a non-conflicting local preview server on port `3002`
  - `http://127.0.0.1:3002/login` returned `200 OK`
- Verification note:
  - port `3000` was already occupied by an existing service, so the preview server uses `3002`
  - this pass changed shared visual UI only; no database schema, permissions, server actions, import/export logic, or PostgreSQL data changed

## 2026-05-07

- User asked to choose a suitable open-design skill and optimize the whole web page.
- Re-read the required memory-bank files and treated the work as a Step 7 stabilization and handover-experience refinement, not a product-scope change.
- Skill choice:
  - used `workflow-orchestration` for the non-trivial execution flow
  - selected `james-design` as the practical UI-design skill because this is an existing data-heavy admin web app; `open-design-landing-deck` is presentation/landing-page oriented and was not appropriate for the school operations backend
- Changes made:
  - revised `src/app/globals.css` from decorative glass/warm background styling to a quieter data-entry backend surface with neutral background, flatter cards, tighter form controls, table affordances, and a defined `--panel-soft` token
  - revised `src/app/layout.tsx` Ant Design tokens so primary color, border radius, container background, buttons, inputs, selects, cards, and tables align with the new admin-density direction
  - revised `src/components/shell/dashboard-shell.tsx` so all dashboard pages now use a tighter sidebar, sticky top status bar, role label, flat content area, and wider working canvas instead of a large nested glass content panel
  - revised `src/app/dashboard/page.tsx` from a hero-style overview into an operations-oriented dashboard with compact module entry tiles, status metrics, module responsibilities, and next-step tracking
- Verification:
  - `npm run lint -- src/app/globals.css src/app/layout.tsx src/components/shell/dashboard-shell.tsx src/app/dashboard/page.tsx` completed with only the expected CSS ignored-file warning
  - `npm run typecheck`
  - `npm run build`
  - `npm run lint`
- Verification note:
  - this pass changed shared visual structure only; no database schema, permissions, server actions, import/export logic, or live PostgreSQL data changed
  - `git status` could not be run because `git` is not available on the current PATH in this shell

- User clarified that the need is not only deleting seeded demo rows, but a visual backend data management center for all already-entered demo or manually entered test data.
- Re-read the required memory-bank files and treated the work as a Step 7 stabilization and operations feature with a product-scope update to PRD assumptions.
- Changes made:
  - added `src/modules/data-management` with grouped table definitions, query helpers, backup helper, server actions, and a system-admin-only page UI
  - added `/dashboard/data-management` with `requireSystemAdmin()` gating
  - exposed the data management entry in the sidebar and dashboard overview for `SYSTEM_ADMIN`
  - implemented table counts, search, pagination, row previews, selected-row deletion, single-row deletion, and read-only audit-log viewing
  - implemented three fixed cleanup actions: `清空检查数据`, `清空师生档案`, and `清空结构与业务数据`
  - added backup-before-delete protection through `pg_dump --format=custom`, with `PG_DUMP_PATH` and `DATA_BACKUP_DIR` overrides
  - wrote backup and delete outcomes into `AuditLog`, including deleted counts and backup file path
  - added `scripts/simulate-data-management.mjs` and `npm run test:data-management` for PGlite preview-count and cleanup-order verification
  - updated `docs/pilot-accounts-and-usage-guide.md`, `memory-bank/prd.md`, `memory-bank/implementation-plan.md`, and `memory-bank/architecture.md`
- Verification completed so far:
  - `npm run lint -- src/modules/data-management src/app/dashboard/data-management/page.tsx src/components/shell/dashboard-shell.tsx src/app/dashboard/page.tsx src/lib/app-config.ts`
  - `npm run typecheck`
  - `npm run lint -- scripts/simulate-data-management.mjs`
  - `npm run test:data-management`
  - real `pg_dump` smoke after stripping Prisma's `schema=public` URL parameter, producing `artifacts/data-management-backups/data_management_backup_smoke.dump`
  - fixed the backup helper so it removes Prisma-only `schema` before calling `pg_dump`
  - full-project `npm run lint`
  - `npm run build`
  - production smoke on `127.0.0.1:3001` with a temporary `next start` job: `admin` received `200` and saw `数据管理中心`; `leader1`, `data.manager`, `inspector`, and `grade11.manager1` were redirected with `307`
- Verification note:
  - the smoke server was temporary and was stopped after the check
  - no delete action was executed against the real PostgreSQL database in this turn

- User asked how to conveniently delete demo data that had been submitted into the system database.
- Re-read the required `memory-bank` files and confirmed this is a Step 7 operations and handoff task, not a product-scope change.
- Inspection result:
  - existing `npm run db:seed:demo` already deletes and rewrites fixed seeded demo IDs before inserting data
  - there was no delete-only command for removing those fixed seeded demo rows permanently
  - manually entered test records do not currently carry an explicit demo marker, so they cannot be safely deleted by a generic "demo data" command without clear filters or a backup restore point
- Changes made:
  - added `scripts/clear-demo-postgres.mjs`
  - added `npm run db:clear:demo` and `npm run db:clear:demo:dry-run`
  - updated README and deployment/runbook notes with the seeded-demo cleanup workflow and the manual-test-data caveat
  - updated `memory-bank/architecture.md` with the new cleanup script boundary
- Verification:
  - `npm run db:clear:demo:dry-run`
  - `npm run lint -- scripts/clear-demo-postgres.mjs`
  - `npm run typecheck`
- Verification note:
  - the real `npm run db:clear:demo` command was not executed in this turn because it would mutate the current PostgreSQL database
  - `git diff` and `git status` could not be run because `git` is not available on the current PATH in this shell

## 2026-05-02

- User then requested a `neat-freak` cleanup pass after the teacher-facing grade-department rollover sync correction.
- Reconciled external docs and project memory with the verified live behavior:
  - updated `README.md`, `docs/deployment-and-smoke-test.md`, `docs/pilot-accounts-and-usage-guide.md`, and `docs/postgresql-install-and-acceptance-runbook.md` so they explicitly describe that cohort rollover also synchronizes teacher-facing grade-department names to the final active cohort set
  - localized the remaining English LAN-address helper sentences in the deployment and PostgreSQL runbooks
  - removed relative-time wording such as `今天` and ambiguous `最近` phrasing from the operator docs where it would age badly or read like stale session context
  - softened one stale `memory-bank/implementation-plan.md` current-status line so it no longer presents `192.168.1.3:3000` as the current restored live address
  - clarified the earlier same-day rollover-sync progress note so it is marked as superseded by the broader final-active-cohort reconciliation rule
- Verification completed for this cleanup:
  - re-read all root and `docs/` markdown files involved in the current rollout handoff
  - searched `README.md`, `docs/`, `memory-bank/`, `tasks/`, and `AGENTS.md` for stale relative-date wording and leftover English helper sentences
  - ran `npm run lint -- README.md docs/deployment-and-smoke-test.md docs/pilot-accounts-and-usage-guide.md docs/postgresql-install-and-acceptance-runbook.md docs/security-and-deployment-plan.md`; ESLint reported only the expected `File ignored because no matching configuration was supplied` warnings for markdown files and no errors

- User corrected the previous cohort-rollover department sync and reported that all teacher-related department names were still not synchronizing correctly.
- Re-read the required `memory-bank` files and confirmed this is still Step 3 school structure rollover behavior with Step 4 teacher-department visibility impact.
- Root cause:
  - the previous implementation only tried to rename the one department whose `XXXX级年级` segment matched the archived or graduating source cohort
  - it returned no updates when no source cohort was archived and did not compare department names against the full final active cohort set after rollover
  - the live database had active cohorts `2024`, `2025`, and `2027`, while teacher grade-department names still showed `2023级年级`, `2024级年级`, and `2025级年级`
- Changes made:
  - replaced the narrow one-grade rename rule with `buildGradeDepartmentNameSyncPlan()` in `src/lib/grade-lifecycle.ts`
  - updated `rolloverAcademicYear` so grade-department names are synchronized against the complete post-rollover active enrollment-year set, not only against archived cohorts
  - kept unrelated departments unchanged and preserved uniqueness checks so existing department names are not overwritten
  - repaired the current live PostgreSQL data by renaming `2023级年级` to `2027级年级` and inserted an audit log entry for that one-time synchronization
  - rebuilt the app and restarted the live workstation site so future rollovers use the broader sync rule
- Verification completed:
  - `npm run typecheck`
  - `npm run lint -- src/lib/grade-lifecycle.ts src/modules/school-structure/actions.ts`
  - `npm run build`
  - live PostgreSQL check confirmed active cohorts are `2024`, `2025`, and `2027`
  - live PostgreSQL check confirmed teacher grade-department names are now `2024级年级`, `2025级年级`, and `2027级年级`
  - restarted the live pilot site and confirmed port `3000` is listening
  - `http://127.0.0.1:3000/login` returns `200`
  - `http://192.168.1.4:3000/login` returns `200`
- Verification note:
  - this fixed current live data and future rollover logic; no teacher, student, grade, class, or department-assignment relationships were changed

- User asked that after executing cohort rollover, department names containing `XXXX级年级` should automatically change with the rollover.
- Re-read the required `memory-bank` files and confirmed the work belongs to Step 3 school structure and cohort rollover behavior.
- Changes made:
  - added shared grade-department name helpers in `src/lib/grade-lifecycle.ts` for parsing and replacing the enrollment year inside department names such as `2023级年级`
  - first updated `rolloverAcademicYear` in `src/modules/school-structure/actions.ts` with a narrow graduating-cohort rename rule; this was later superseded the same day by the broader final-active-cohort synchronization rule recorded above
  - recorded any automatic department-name changes in the rollover audit metadata
- Verification completed:
  - `npm run typecheck`
  - `npm run lint -- src/lib/grade-lifecycle.ts src/modules/school-structure/actions.ts`
  - `npm run build`
- Verification gap:
  - this pass did not execute a live cohort rollover or mutate the current PostgreSQL department rows; it verifies the code path and production build only
- Next recommended action:
  - when the school next performs an actual cohort rollover, confirm the structure page department list includes the new active grade-department name and no longer keeps the just-graduated `XXXX级年级` department name

- User reported that after moving the workstation to another network, the old URL `http://192.168.1.3:3000` no longer opened, and asked for the workstation plus same-network devices to keep working after future network changes.
- Re-read the required `memory-bank` files and confirmed the work belongs to Step 7 stabilization, deployment, and handover.
- Root cause:
  - the workstation's current LAN IPv4 address is now `192.168.1.4`, so `192.168.1.3` is stale and should not be treated as a permanent identity
  - the pilot app already listens on `0.0.0.0:3000`, but login success previously followed the absolute URL returned by NextAuth, which can be tied to a stale `NEXTAUTH_URL`
  - the startup mutex was held for the full lifetime of the long-running `next start` process, so rerunning the launcher while the service was already up could be blocked before it refreshed the current LAN URL
- Changes made:
  - updated `src/components/auth/login-form.tsx` so successful credentials login routes to the safe relative callback path instead of the absolute URL returned by NextAuth
  - updated `scripts/start-school-pilot.ps1` so it writes `logs/current-school-pilot-url.txt` with the current local, LAN, and computer-name URL hints
  - updated `scripts/start-school-pilot.ps1` so the startup mutex is released before entering the long-running `npm run start`, allowing later `-CheckOnly` or launcher runs to refresh the current URL while the service is already running
  - updated `docs/pilot-accounts-and-usage-guide.md` so the operator guide no longer hard-codes `192.168.1.3` as the fixed pilot address
- Verification completed:
  - `ipconfig` showed the current Ethernet IPv4 address is `192.168.1.4`
  - `npm run lint -- src/components/auth/login-form.tsx`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - PowerShell syntax parse for `scripts/start-school-pilot.ps1`
  - restarted the live workstation service outside the sandbox through `scripts/autostart-school-pilot.cmd`
  - `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3000/login` returns `200 OK`
  - `Invoke-WebRequest -UseBasicParsing http://192.168.1.4:3000/login` returns `200 OK`
  - `scripts/start-school-pilot.ps1 -NoInstall -SkipBuild -CheckOnly` now succeeds while the service is running and prints the current same-network URL
- Current access guidance:
  - the workstation itself should open `http://127.0.0.1:3000/login`
  - devices on the same current network should open `http://192.168.1.4:3000/login`, or read `SameNetworkLoginUrl` from `logs/current-school-pilot-url.txt` after future network changes
- Remaining caveat:
  - if another same-network device still cannot connect while the workstation gets `200 OK`, the next likely cause is Windows Firewall or the network profile blocking inbound TCP `3000`

- User then asked for a `neat-freak` cleanup pass after the LAN-change hardening.
- Reconciled the operator docs and task memory with the current verified network-change behavior:
  - updated `README.md` so it now points operators to `logs/current-school-pilot-url.txt` for the current LAN URL instead of relying on a remembered fixed workstation IP
  - updated `docs/deployment-and-smoke-test.md` and `docs/postgresql-install-and-acceptance-runbook.md` so they both mention the generated current-URL note and no longer describe browser-use login as bouncing back to an old LAN host
  - updated `memory-bank/implementation-plan.md` so the next recommended action now focuses on LAN reachability and Windows Firewall checks rather than the already-fixed browser-use loopback bounce
  - updated `tasks/todo.md` to record the completed LAN-change hardening follow-up
- Verification completed for this cleanup:
  - searched `README.md`, `docs/`, `memory-bank/`, and `tasks/` for stale `192.168.1.3`, `NEXTAUTH_URL`, and browser-use bounce references
  - `npm run lint`
  - confirmed the live service still listens on `0.0.0.0:3000`
  - reconfirmed `http://127.0.0.1:3000/login` and `http://192.168.1.4:3000/login` both return `200 OK`

## 2026-05-01

- User asked for a Step 5 routine-inspection form refinement in `src/modules/inspection`:
  - student quantification record entry should only show classes under the selected grade
  - teacher quantification record entry should let operators narrow teachers by department and or subject before choosing the teacher
- Re-read the required `memory-bank` files before editing and confirmed the request belongs to Step 5 routine inspection rather than a scope change.
- Updated the inspection record-entry UI:
  - added `src/modules/inspection/record-scope-fields.tsx` as a small client-side dependent-selector component
  - changed the student record form so grade selection drives the class dropdown and invalid class selections are cleared immediately
  - changed the teacher record form so department and subject selectors narrow the teacher dropdown, while existing edit forms keep the current teacher visible
  - kept the existing server actions and scope validation unchanged so grade-manager and teacher-quantification permission boundaries still rely on the verified server-side checks
- Verification completed:
  - `npm run typecheck`
  - `npm run lint -- src/modules/inspection/page.tsx src/modules/inspection/record-scope-fields.tsx`
  - `npm run build`
- Verification gap:
  - this pass did not include a browser click-through of `/dashboard/inspection`, so the new selector behavior is verified by static checks rather than live manual interaction
- Next recommended action:
  - when convenient, do one quick live `/dashboard/inspection` smoke pass to confirm the new dependent dropdown behavior feels right for operators

- User explicitly requested a `neat-freak` cleanup pass after the live login and business-page recovery work.
- Reconciled operator docs and task memory with the current verified workstation state:
  - updated `README.md` to distinguish stale Next chunk failures from Prisma schema-drift failures when pages show `This page couldn't load`
  - updated `docs/deployment-and-smoke-test.md`, `docs/pilot-accounts-and-usage-guide.md`, and `docs/postgresql-install-and-acceptance-runbook.md` with the business-page `server error` recovery path for Prisma `P2022 ColumnNotFound`
  - updated `docs/security-and-deployment-plan.md` so it no longer says real PostgreSQL browser smoke, backup, or restore rehearsal is still missing
  - updated `tasks/todo.md` to mark the stable live process hardening and schema-drift recovery as completed follow-ups
  - updated `tasks/lessons.md` with lessons for schema drift on data-heavy pages and duplicate Windows launcher starts
- Verification completed for this cleanup:
  - searched README, docs, tasks, and memory-bank files for stale rollout claims and relative-date wording
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-school-pilot.ps1 -PublicHost 192.168.1.3 -NoInstall -SkipBuild -CheckOnly`
  - `npm run typecheck`
- Verification gap:
  - this cleanup was documentation and memory reconciliation only; no business code, database rows, or live browser state changed in this pass

- User then confirmed `http://192.168.1.3:3000` opens in the browser, but reported that `师生档案`, `往届存档`, `常规检查`, and `统计导出` all show `This page couldn't load / A server error occurred`.
- Re-read the required `memory-bank` files and confirmed this is still a Step 7 live-stabilization issue affecting the Step 4, Step 5, and Step 6 business pages.
- Reproduced the authenticated failure path against the live workstation app:
  - unauthenticated requests to the four pages correctly redirected to `/login`
  - authenticated requests to `/dashboard/people`, `/dashboard/archive/students`, `/dashboard/inspection`, and `/dashboard/exports` aborted with the server closing the connection
  - a temporary logged 3001 production instance captured the real server error: Prisma `P2022 ColumnNotFound` from `Teacher`, `Student`, and `InspectionRecord` queries
- Root cause:
  - the live Next.js build expected the current Prisma schema, but the workstation PostgreSQL database was missing newer columns such as teacher and student `idCardNumber` and profile-field `fieldKey`
  - this schema drift broke the data-heavy business pages while `/login` still worked
- Recovery completed:
  - first tried `npm run db:push`; Prisma required explicit acknowledgement because it would add unique constraints
  - checked the live database first and found the referenced columns did not exist yet, so there were no existing duplicate values to conflict with those new unique constraints
  - ran `prisma db push --accept-data-loss` against the live local PostgreSQL database, which completed successfully and reported the database is now in sync with the Prisma schema
- Verification completed:
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-school-pilot.ps1 -PublicHost 192.168.1.3 -NoInstall -SkipBuild -CheckOnly`
  - direct PostgreSQL column check confirmed the live database now has `Teacher.idCardNumber`, `Student.idCardNumber`, and `ProfileFieldDefinition.fieldKey`
  - authenticated live page checks now return `200` for `/dashboard/people`
  - authenticated live page checks now return `200` for `/dashboard/archive/students`
  - authenticated live page checks now return `200` for `/dashboard/inspection`
  - authenticated live page checks now return `200` for `/dashboard/exports`
  - all four checked HTML responses were verified not to contain the Next server-error text
  - `npm run typecheck`
- Verification note:
  - no live process restart was needed after the schema sync because the existing `0.0.0.0:3000` process recovered once PostgreSQL had the expected columns

- User reported that `http://192.168.1.3:3000/login` could not open normally.
- Re-read the required `memory-bank` files before touching code and confirmed the issue belongs to Step 7 stabilization, deployment, and handover.
- Root cause:
  - PostgreSQL was running normally as the `postgresql-xiaowuxitong` Windows service.
  - `.env.local` still had `NEXTAUTH_URL=http://192.168.1.3:3000`.
  - nothing was listening on TCP `3000`, so the live web application process was offline.
  - the first recovery attempt through `scripts/autostart-school-pilot.cmd` exposed a duplicate-start race where two launcher instances tried to rewrite `.env.local` at the same time and both failed.
- Recovery completed:
  - started the live Next.js production service on `0.0.0.0:3000`.
  - confirmed both `http://127.0.0.1:3000/login` and `http://192.168.1.3:3000/login` return `200 OK`.
  - confirmed the first batch of `/_next/static` assets referenced by `/login` also return `200`, so the browser should not hit the earlier chunk-load failure mode.
- Hardening completed:
  - updated `scripts/start-school-pilot.ps1` with a named startup mutex so duplicate launcher instances no longer race over `.env.local`.
  - added an existing-port check so the launcher exits cleanly when port `3000` is already serving the pilot site.
- Verification completed:
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-school-pilot.ps1 -PublicHost 192.168.1.3 -NoInstall -SkipBuild -CheckOnly`
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-school-pilot.ps1 -PublicHost 192.168.1.3 -NoInstall -SkipBuild`
  - `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3000/login`
  - `Invoke-WebRequest -UseBasicParsing http://192.168.1.3:3000/login`
  - direct static-asset checks for the login page's `/_next/static` resources
- Verification gap:
  - browser-use visual verification could not run because the browser plugin still failed to start its app-server path; HTTP and static-resource checks passed from the workstation.
- Next recommended action:
  - ask one office PC on the school LAN to refresh `http://192.168.1.3:3000/login`; if it still cannot open while the workstation returns `200`, the next suspect is the client machine's network path or Windows Firewall profile rather than the app process.

- User asked to check whether the Step 4 requirement was complete:
  - former fixed statistics fields, such as teacher employee number and student number, should be addable, enableable or disableable, and deleteable through the profile-category workflow
  - spreadsheet import updates and unique identification should be handled by identity card number
- Re-read the required `memory-bank` files before editing and confirmed the work belongs to Step 4 people records.
- Inspection result:
  - the core implementation was already present: teacher and student forms require `idCardNumber`, Prisma has unique identity-card-number constraints, and teacher or student imports use `findUnique({ idCardNumber })` to update existing records
  - teacher employee number, student number, gender, phone, duties, guardian contact, and remarks are bridged through `ProfileFieldDefinition.fieldKey`
  - deleting a bridged system category marks it inactive and deleted so it is removed from forms, templates, and exports without being automatically recreated by later profile-field queries
  - custom profile categories remain hard-deleted with stored JSON values pruned
- Documentation and task records updated:
  - `memory-bank/prd.md`
  - `memory-bank/implementation-plan.md`
  - `memory-bank/architecture.md`
  - `tasks/todo.md`
- Fixed one uncovered verification issue:
  - `npm run test:demo-db` initially failed because active and alumni demo students reused the same generated identity-card-number sequence, which now violates the intended `Student.idCardNumber` uniqueness rule
  - updated `scripts/demo-data.mjs` so active demo students use the 2009 birth-year identity-card-number prefix while alumni demo students use 2006, preserving unique identity-card numbers across active and archived student records
- Verification completed:
  - `npm run typecheck`
  - `npm run lint`
  - `npm run build`
  - `npm run db:validate`
  - `npm run db:seed:demo:dry-run`
  - `npm run test:demo-db`
- Verification note:
  - this pass did not push to or mutate the live workstation PostgreSQL database because no schema change was required and the only code change was deterministic demo data generation
- Next recommended action:
  - if live visual confidence is needed after restart, do a quick `/dashboard/people` browser smoke to confirm the currently active categories, template headers, and imports still match the verified code path

## 2026-04-30

- User asked for two Step 4/Step 3 refinements in the people records area:
  - all teacher or student information statistics categories should be freely addable, disableable, deleteable, and maintainable
  - teacher grade-department names should stop using `高X年级部` and instead use enrollment-year wording such as `2025级年级`
- Re-read the required `memory-bank` files before editing and confirmed the work belongs mainly to Step 4 people records, with a small Step 3 school-structure master-data naming adjustment.
- Updated the people records module:
  - added `deleteProfileFieldDefinition`
  - added a validation schema for profile-category deletion
  - exposed a `删除类目` control next to the existing category name-save and enable/disable controls
  - made category deletion hard-delete the `ProfileFieldDefinition` row and prune that field id from the matching teacher or student `profileData` JSON
- Updated department naming sources:
  - changed shared demo department names from `2025级年级部` style to `2025级年级` style
  - changed the teacher import template example from `教务处、2025级年级部` to `教务处、2025级年级`
  - updated the live workstation PostgreSQL departments from `高一年级部 / 高二年级部 / 高三年级部` to `2025级年级 / 2024级年级 / 2023级年级`
- Restarted the live pilot site after the production build so the running `/dashboard/people` page picked up the new delete-category button.
- Verification completed:
  - `npm run typecheck`
  - `npm run lint -- src/modules/people/actions.ts src/modules/people/page.tsx src/modules/people/routes.ts src/lib/validation/people.ts`
  - `npm run build`
  - `npm run test:demo-db`
  - `npm run db:seed:demo:dry-run`
  - direct PostgreSQL check confirmed no remaining department names matching `年级部`
  - live `/login` returned `200` on both `127.0.0.1:3000` and `192.168.1.3:3000`
  - authenticated live HTML check confirmed `/dashboard/people` contains `删除类目`, contains enrollment-year department wording, and no longer contains old `年级部` wording
- Verification gap:
  - this pass used authenticated HTML checks rather than a manual browser click-through for pressing the delete button
- Next recommended action:
  - if operators want extra caution before deleting real categories, add a small confirmation dialog around `删除类目`; the current implementation already performs the requested delete behavior server-side

## 2026-04-29

- User explicitly asked for a `neat-freak` cleanup pass, so I re-read the required `memory-bank` files plus `README.md`, `docs/deployment-and-smoke-test.md`, and `tasks/todo.md` to reconcile stale status notes against the already verified live workstation state.
- Cleaned the stale project-memory mismatches instead of adding another contradictory note:
  - `memory-bank/implementation-plan.md` no longer says Step 3 through Step 6 browser smoke is still pending, and it now reflects the completed low-risk compatibility follow-up plus the completed browser-driven UI smoke scope
  - `docs/deployment-and-smoke-test.md` no longer claims that real PostgreSQL browser smoke, backup or restore rehearsal, or localhost PostgreSQL detection are missing; that section now records the current workstation caveats instead
  - `memory-bank/architecture.md` now makes it explicit that the remaining follow-ups are rollout or operator items rather than missing core module work
- Verification completed for this cleanup:
  - re-read the current `README.md`, `docs/deployment-and-smoke-test.md`, `tasks/todo.md`, `memory-bank/implementation-plan.md`, `memory-bank/progress.md`, and `memory-bank/architecture.md`
  - cross-checked the stale statements against the already recorded live smoke, PostgreSQL rollout, backup/restore rehearsal, auto-start, and browser-use caveat entries in `memory-bank/progress.md`
- Verification gap:
  - this pass was documentation-only; no runtime code, database rows, or live browser state changed
- Next recommended action:
  - keep using `tasks/todo.md` as the single source for the few remaining rollout items instead of leaving old `pending smoke` statements in the planning docs

- User reported that `browser-use` should be back, so I retried the in-app browser path before falling back to any non-visual checks.
- Confirmed the browser-use runtime itself is usable again on loopback URLs, then found the remaining workstation-specific caveat:
  - `setupAtlasRuntime` now succeeds
  - `http://127.0.0.1:3000/login` opens correctly in the in-app browser
  - after a successful credentials login, the live pilot still redirects to `http://192.168.1.3:3000/...` because the workstation launcher rewrites `NEXTAUTH_URL` to the LAN host
  - browser-use treats that LAN host as a non-loopback site, so the practical smoke workaround is to log in on `127.0.0.1`, then immediately bounce the tab back to `http://127.0.0.1:3000/dashboard` where the local session cookie is already present
- Completed the first true visual click-through smoke in the in-app browser on the live workstation deployment:
  - `admin` can open `/dashboard/structure`, and the visible form still exposes only grade-first fields such as `targetEnrollmentYear`, `newCohortClassCount`, `enrollmentYear`, and `targetCount`, while `startDate`, `endDate`, and `academicYearId` remain absent
  - `admin` can open `/dashboard/people`, where the teacher multi-department create form visibly renders the department checkbox group
  - `admin` can click into statistics-category creation and see the inline category-create UI render on the live page
  - `admin` can click `导入教师` and reach the live file-picker import UI without needing a fallback flow
  - `admin` can click the `教师量化` switchers on both `/dashboard/inspection` and `/dashboard/exports`, and the live page shape flips from `gradeId/classId` fields to `teacherId`
- Completed the browser-side role smoke on the same live deployment:
  - `leader1` is still redirected away from `/dashboard/structure` to `/dashboard?forbidden=1`
  - `leader1` can still open `/dashboard/people`, `/dashboard/inspection`, and `/dashboard/exports`
  - `leader1` inspection remains visually read-only because `inspectionDate` entry controls are absent even though filters are available
  - `grade11.manager1` is still redirected away from `/dashboard/structure`
  - `grade11.manager1` people management hides teacher import/create controls, hides statistics-category configuration, and keeps teacher multi-department controls out of the visible page
  - `grade11.manager1` inspection and export pages do not render the `教师量化` switcher, and even direct `?targetType=TEACHER` URLs still render only student-scoped `gradeId/classId` fields with no `teacherId`
- Updated project records after the visual smoke:
  - `tasks/todo.md`
  - `memory-bank/implementation-plan.md`
  - `memory-bank/architecture.md`
- Verification gap:
  - browser-use is restored for local loopback testing, but the workstation's LAN-oriented `NEXTAUTH_URL` still makes visual QA awkward because Auth.js bounces the tab from `127.0.0.1` to `192.168.1.3` after login
- Next recommended action:
  - if future browser-use regression or UX checks should stay fully smooth, align the pilot's canonical browser host with a browser-use-friendly loopback or localhost path during workstation-only QA sessions

- User asked to keep going, so I completed the previously recommended live workstation smoke for the low-risk grade-first compatibility rollout.
- Attempted to use the in-app `browser-use` runtime first because this follow-up was a local browser test, but the runtime failed to start with a local app-server path error even though the plugin files were present.
- Confirmed this was a browser-channel problem rather than a site outage:
  - `http://192.168.1.3:3000/login` returned `200`
  - `http://127.0.0.1:3000/login` returned `200`
- Completed the live smoke through authenticated real-page requests with the same workstation deployment instead of guessing from source code:
  - authenticated `admin`, `leader1`, and `grade11.manager1` through the real NextAuth credentials callback
  - added the required `school-browser-session=active` cookie so the browser-bound session guard matched real runtime behavior
  - fetched the live HTML for `/dashboard/structure`, `/dashboard/people`, `/dashboard/inspection`, `/dashboard/inspection?targetType=TEACHER`, `/dashboard/exports`, and `/dashboard/exports?targetType=TEACHER`
- Live verification confirmed the grade-first compatibility behavior:
  - `admin` can open `/dashboard/structure`, and the live page exposes `targetEnrollmentYear`, `newCohortClassCount`, `enrollmentYear`, and `targetCount` inputs while not exposing `startDate`, `endDate`, or `academicYearId` form fields
  - `admin` can open `/dashboard/people`, where live HTML still contains teacher and student template routes plus the multi-department `departmentIds` control, and no visible academic-year wording appears in the active page
  - `admin` can switch `/dashboard/inspection` and `/dashboard/exports` between student quantification and teacher quantification; the student view exposes `gradeId/classId`, while the teacher view exposes `teacherId` without `gradeId/classId`
- Live verification confirmed the role boundaries still hold after the compatibility pass:
  - `leader1` requesting `/dashboard/structure` is redirected to `/dashboard?forbidden=1`
  - `leader1` can open `/dashboard/people`, `/dashboard/inspection`, and `/dashboard/exports`
  - `leader1` inspection pages are read-only in live HTML: filter controls remain available, but `inspectionDate` record-entry fields are absent in both student and teacher quantification views
  - `grade11.manager1` requesting `/dashboard/structure` is redirected to `/dashboard?forbidden=1`
  - `grade11.manager1` can open `/dashboard/people`, but the live page hides teacher-template and multi-department editing controls while keeping the student template path available
  - `grade11.manager1` live inspection and export views stay student-scoped by rendered fields: even when the request URL carries `targetType=TEACHER`, the returned page still renders `gradeId/classId` and does not render `teacherId`
- Updated project records after verification:
  - `tasks/todo.md`
  - `memory-bank/implementation-plan.md`
  - `memory-bank/architecture.md`
- Verification gap:
  - this pass still was not a true visual click-through because the `browser-use` runtime failed before the first browser action
- Next recommended action:
  - once the in-app browser runtime is healthy again, run one true visual smoke for teacher multi-department edit, statistics-category creation, import upload, and one click-through of the student-versus-teacher inspection or export switchers

- User resumed the interrupted low-risk de-academic-year compatibility pass and explicitly requested a docs or memory cleanup finish, so I re-read the required `memory-bank` files plus the workflow-orchestration and neat-freak guidance before touching code or docs.
- Re-checked the active structure route instead of trusting the earlier summary and confirmed `/dashboard/structure` still renders `src/modules/school-structure/page.tsx`; the real page is already grade-first, so this continuation needed verification and documentation reconciliation rather than a new route/component swap.
- Re-verified the current local repo state for the compatibility pass:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run test:demo-db`
- Reconciled source-of-truth and operator docs with the verified repo state:
  - updated `README.md` so the current module summary reflects all six live business modules
  - updated `docs/deployment-and-smoke-test.md` so the seeded demo counts now match the current simulation output, including profile-field definitions plus the split inspection category, item, and record totals
  - updated `memory-bank/implementation-plan.md` to mark the low-risk compatibility pass as completed in code, verification, and docs
  - updated `memory-bank/architecture.md` to state that the active structure route still uses the grade-first `SchoolStructurePage` surface while compatibility `AcademicYear` handling remains server-side
  - corrected one stale structure-page component note below in this same `progress.md` entry so the historical record matches the real repository state
- Verification gap:
  - this continuation had not yet run the recommended live workstation smoke at that point; the later entry above records the completed fallback-based smoke result
- Next recommended action:
  - continue with click-level visual smoke once the in-app browser runtime is healthy again

- User asked to continue, so I moved the new Step 4 people-records extension from static verification into live PostgreSQL rollout and smoke validation.
- Re-read the required `memory-bank` files again before changing the live environment and confirmed this follow-up belongs to Step 4 verification plus Step 7 stabilization.
- Applied the new people-records schema to the live workstation PostgreSQL database:
  - ran `npm run db:push`
  - confirmed the database is now in sync with the new Prisma schema that includes `TeacherDepartmentAssignment`, `ProfileFieldDefinition`, and teacher or student `profileData`
- Found that the live database had no configured profile-field definitions yet, so current live import templates correctly contained only the fixed columns until real categories were added.
- Added the first live category definitions directly into PostgreSQL so the new workflow is immediately usable:
  - teacher: `办公室`, `职称`
  - student: `宿舍信息`, `生源地`
  - used idempotent upsert-style SQL behavior so reruns enable or reorder the same fields instead of duplicating them
- Live smoke validation had to avoid the in-app browser path this turn:
  - the browser-use plugin runtime failed to start in this environment
  - the normal detached live process launch path was flaky from the current sandbox shell, so I used the existing `scripts/autostart-school-pilot.cmd` launcher and completed the smoke checks inside the same shell session while the site was up
- Live verification completed for the new people-records rollout:
  - `/login` returned `200`
  - `/api/auth/csrf` returned a valid token payload
  - authenticated `admin` access to `/dashboard/people` returned `200`
  - live admin HTML now contains `教师档案`, `学生档案`, `统计类目配置`, the multi-department checkbox name `departmentIds`, and the student template route link
  - live teacher template download now includes `办公室` and `职称`
  - live student template download now includes `宿舍信息` and `生源地`
  - authenticated `grade11.manager1` access to `/dashboard/people` still hides the teacher-management area and the statistics-category config area while keeping the student area visible
- Verification completed for this live follow-up:
  - `npm run db:push`
  - direct PostgreSQL insert or update checks for the four initial profile-field definitions
  - authenticated live HTML checks for `admin`
  - authenticated live template download and workbook-header inspection for teacher and student templates
  - authenticated live role-scope check for `grade11.manager1`
- Verification gap:
  - this turn did not click the new statistics-category create form in a real browser because the browser-use runtime was unavailable
  - this turn did not perform a live teacher edit save that writes a second department affiliation through the UI
  - this turn did not run a live import upload using the new dynamic template columns
- Next recommended action:
  - do one true browser-interaction smoke for category creation, teacher multi-department save, and one import upload after the browser runtime issue is resolved

- User added two new Step 4 requirements inside teacher and student records:
  - teachers must be able to belong to multiple departments at the same time
  - teacher or student information statistics categories must be configurable inside the system, and downloaded import templates must include all currently active categories such as student dormitory information
- Re-read the required `memory-bank` files before editing and confirmed this belongs to the Step 4 people-records extension rather than a new module.
- Updated source-of-truth docs before code changes:
  - `memory-bank/prd.md`
  - `memory-bank/implementation-plan.md`
  - `tasks/todo.md`
- Extended the schema and people data model:
  - added `TeacherDepartmentAssignment` so a teacher can belong to multiple departments while the legacy `Teacher.departmentId` compatibility field stays aligned to the first selected department
  - added `ProfileFieldDefinition`
  - added `Teacher.profileData` and `Student.profileData` JSON storage for configurable information statistics values
- Reworked the people module:
  - teacher create, edit, import, export, and display flows now support multiple department affiliations
  - added in-system teacher or student statistics-category maintenance on `/dashboard/people`
  - active category definitions now render as live form inputs on the teacher page, the active student page, and the alumni archive page
  - active people and archive imports now merge dynamic category values safely so stale templates do not erase newer fields that were not present in the uploaded workbook
  - teacher or student template downloads and exports now build headers from the current active category definitions
- Updated downstream consistency points that show teacher department context:
  - inspection teacher selectors now show multi-department labels
  - reporting teacher selectors and scope summaries now show multi-department labels
- Refreshed verification artifacts so the new structure is testable without a live PostgreSQL rollout:
  - rewrote `scripts/demo-data.mjs` with clean seed text plus teacher multi-department assignments and configurable profile-field examples
  - updated `scripts/seed-demo-postgres.mjs` dry-run planning for `ProfileFieldDefinition` and `TeacherDepartmentAssignment`
  - updated `scripts/simulate-school-affairs.mjs` schema and seed flow for the new people-records structure
- Verification completed for this Step 4 extension:
  - `npm run db:generate`
  - `npm run typecheck`
  - `npm run lint`
  - `npm run build`
  - `npm run db:validate`
  - `npm run db:seed:demo:dry-run`
  - `npm run test:demo-db`
- Verification gap:
  - this turn did not push the new people-records schema to the live workstation PostgreSQL database
  - this turn did not run a live browser smoke for `/dashboard/people` or `/dashboard/archive/students` after the new dynamic categories and multi-department UI changes
- Next recommended action:
  - apply the new schema to the live workstation database with `npm run db:push`
  - do one live browser smoke covering teacher multi-department edit, student dormitory category creation, template download, and import or export behavior

## 2026-04-28

- User asked to tighten the statistics export content so exported inspection files carry target-type-specific detail columns instead of only aggregate summary rows.
- Re-read the required `memory-bank` files before editing and confirmed this belongs to Step 6 statistics and export refinement.
- Updated `src/modules/reporting/export.ts` only for the code change:
  - Excel exports now keep the existing summary sheets and add a `检查明细` sheet
  - student quantification detail rows now export `记录日期 / 检查项目 / 年级 / 班级 / 结果数值 / 备注内容`
  - teacher quantification detail rows now export `记录日期 / 检查项目 / 教师姓名 / 结果数值 / 备注内容`
  - CSV exports now emit the same target-type-specific detail rows so the flat file columns match the requested inspection content
- Verification completed for this export-content adjustment:
  - `npm run typecheck` via `npm.cmd`
  - `npm run lint -- src/modules/reporting/export.ts` via `npm.cmd`
- Verification gap:
  - this turn did not run a browser download smoke or open a generated workbook visually
- Next recommended action:
  - do one live export download from `/dashboard/exports` for both student quantification and teacher quantification to visually confirm the final workbook and CSV column order on the workstation

- User asked to continue after the student-versus-teacher quantification refactor, specifically to carry the change through local PostgreSQL application and smoke validation.
- Re-read the required `memory-bank` files before doing more work and confirmed this follow-up belongs to Step 7 stabilization and verification.
- Applied the inspection schema change to the current PostgreSQL database:
  - first checked that `InspectionCategory` had no duplicate names that would block the new `[targetType, name]` uniqueness
  - ran `npm run db:push -- --accept-data-loss`
- Found live legacy data that needed migration care instead of a blind schema-only rollout:
  - the database already contained a teacher-oriented category `教师发展中心检查`
  - that category already had one old record stored in the pre-split class-based shape
- Added a repeatable helper script for this rollout:
  - `scripts/backfill-inspection-target-types.mjs`
  - `package.json` script: `npm run db:backfill:inspection-targets`
- Ran the live backfill and confirmed:
  - `教师发展中心检查` is now marked as teacher quantification
  - one legacy record remains intentionally visible as an orphan teacher-quantification record that still needs a manual teacher binding later
- Rebuilt confidence in runtime verification despite the in-app browser plugin failing to launch its local runtime this turn:
  - restarted a temporary `next start` session against the current build inside the same verification command
  - used real NextAuth credential login plus the browser-session cookie flow to fetch the live rendered inspection and export pages for multiple roles
- Browser-equivalent verification completed:
  - `admin` inspection student view contains student-only grade/class fields and exposes teacher-quantification switching
  - `admin` teacher-quantification inspection view contains `teacherId` fields, excludes `gradeId/classId`, and surfaces the migrated teacher category, item, and orphan legacy record
  - `admin` teacher-quantification export view contains teacher-scoped filtering and the teacher export route returns a teacher-quantification CSV filename
  - `leader1` can also reach teacher-quantification inspection and export views with teacher-scoped fields and teacher export routing
  - `grade11.manager1` does not see the teacher-quantification entry from the student view
  - `grade11.manager1` requesting `targetType=TEACHER` is effectively forced back onto the student-scoped page shape with `gradeId/classId` fields
  - `grade11.manager1` teacher-target CSV requests are effectively downgraded to the student-quantification export filename, confirming the server-side scope guard still wins over query parameters
- Verification completed for this Step 7 follow-up:
  - duplicate-category safety query against live PostgreSQL
  - `npm run db:push -- --accept-data-loss`
  - `npm run db:backfill:inspection-targets`
  - role-based live rendered page checks for `admin`, `leader1`, and `grade11.manager1`
  - live teacher export route checks through authenticated HTTP sessions
- Verification gap:
  - the in-app browser plugin runtime failed to start this turn, so verification used authenticated live HTML and export-route checks instead of visible interactive browser screenshots
  - the one legacy teacher-centered record still needs a human decision about which teacher it belongs to
- Next recommended action:
  - open the teacher-quantification page as an admin and manually rebind the old `教师发展中心检查 / 考试监考检查` record to the correct teacher
  - if needed later, run one more true visual smoke pass after the in-app browser plugin runtime issue is resolved

## 2026-04-27

- User requested that the routine inspection module be split into two top-level flows: student quantification and teacher quantification, with teacher results recorded under individual teachers instead of under classes.
- Updated `memory-bank/prd.md` and `memory-bank/implementation-plan.md` before code changes so the new inspection granularity and reporting scope were explicit.
- Extended the inspection data model:
  - added `InspectionTargetType`
  - added `InspectionCategory.targetType`
  - added `InspectionRecord.teacherId`
  - linked teacher quantification records directly to `Teacher`
- Reworked the routine inspection module so `/dashboard/inspection` now:
  - separates student quantification and teacher quantification through top-level view switching
  - keeps student quantification on `grade/class`
  - keeps teacher quantification on `teacher`
  - blocks grade-scoped accounts from teacher quantification access
  - validates that student records cannot point at teachers and teacher records cannot point at grades or classes
- Reworked the statistics and export module so `/dashboard/exports` now:
  - switches filters and summaries between student and teacher quantification
  - aggregates teacher quantification by teacher instead of by class
  - exports target-type-specific xlsx/csv files
- Updated demo data, PostgreSQL demo seed dry-run compatibility, and the PGlite simulation so both quantification flows remain covered by local verification artifacts.
- Verification completed for this inspection split:
  - `npm run db:generate`
  - `npm run typecheck`
  - `npm run lint`
  - `npm run build`
  - `npm run test:demo-db`
  - `npm run db:validate`
  - `npm run db:seed:demo:dry-run`
- Verification gap:
  - no live browser smoke was run against `/dashboard/inspection` or `/dashboard/exports` in this turn
  - no real PostgreSQL `db:push` was applied in this turn
- Next recommended action:
  - run a live browser smoke on `/dashboard/inspection` and `/dashboard/exports`
  - decide when to apply the schema update to the live workstation database with `npm run db:push`

- User approved a follow-up documentation pass after a local login-page incident exposed that the running `next start` process could drift from the latest `.next` build.
- Reproduced the symptom on `http://localhost:3000/login`: the browser showed `This page couldn’t load` while direct HTML requests still returned `200`.
- Captured the concrete runtime error: `ChunkLoadError` for `/_next/static/chunks/04a9d-zxikudm.js`, which was being referenced by the login page but returned `404` from the running site process.
- Confirmed the root cause was a stale live `next start` process serving old HTML against newer build assets; restarting the site restored the missing chunk and brought `/login` back to a healthy browser-rendered state.
- Updated operator-facing docs with the symptom, cause, and recovery steps:
  - `README.md`
  - `docs/deployment-and-smoke-test.md`
  - `docs/pilot-accounts-and-usage-guide.md`
- Verification completed for this documentation/ops slice:
  - confirmed `http://localhost:3000/login` returned `200` after the restart
  - confirmed `/_next/static/chunks/04a9d-zxikudm.js` returned `200` after the restart
  - confirmed the in-app browser login page rendered normally again after reload

- User requested a codebase cleanup so all retained functionality stays inside six business modules only: school structure, user permissions, people records, alumni archive, routine inspection, and statistics export.
- Updated `tasks/todo.md` and `memory-bank/implementation-plan.md` before code changes so the six-module restructuring scope was explicit.
- Standardized the retained modules around module-owned code:
  - moved the retained business page components to `src/modules/*/page.tsx`
  - moved people, alumni-archive, and reporting export/template handlers into module-owned `routes.ts`
  - kept `src/app/dashboard/**` as thin route entry points only
- Removed the showcase/local-demo feature surfaces:
  - deleted `/dashboard/showcase`
  - deleted `/dashboard/demo` and all nested demo export/template routes
  - deleted `src/lib/local-demo-data.ts`
  - deleted `start-local-demo.cmd`
  - deleted `scripts/start-local-demo.ps1`
  - removed `demo:local` from `package.json`
- Updated shared shell and overview entry points so only the six business modules remain visible:
  - `src/components/shell/dashboard-shell.tsx`
  - `src/lib/app-config.ts`
  - `src/app/dashboard/page.tsx`
- Updated `README.md` and architecture records to match the six-module structure, and removed demo-only docs that no longer belong to the product surface:
  - `docs/local-demo-runbook.md`
  - `docs/leader-demo-package.md`
  - `docs/leader-demo-script.md`
- Verification completed for the six-module cleanup:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
- Verification note:
  - `typecheck` initially failed because `.next/dev/types` still contained stale entries for the deleted demo/showcase routes; deleting `.next` and rerunning verification resolved the generated-artifact mismatch

- User confirmed the approved low-risk de-academic-year compatibility pass should continue: visible workflows must stop exposing academic-year management, while the `AcademicYear` table remains as an internal compatibility layer.
- Updated `memory-bank/prd.md` and `memory-bank/implementation-plan.md` before code changes so the new grade-first workflow scope is explicit.
- Refactored the visible school-structure snapshot to return flat active grades plus classes, departments, and subjects; `/dashboard/structure` continues to use server-managed compatibility rollover logic without re-exposing academic-year CRUD.
- Kept the active `/dashboard/structure` route on `src/modules/school-structure/page.tsx` and refactored that live page itself to be grade-first, so the old academic-year UI no longer remains in the active route path.
- Refactored people, inspection, reporting, and user-management option queries so visible grade and class selectors now load directly from `Grade` and `Class` instead of grouped academic-year data.
- Removed academic-year dependence from active-student import/export workflows:
  - student import now resolves rows by grade and class only
  - active student export no longer includes an academic-year column
  - demo student template and demo student export now match the live grade/class-only shape
- Updated visible copy across the dashboard overview, sidebar helper text, structure page, inspection page, reporting page, app config, and local demo so the front-end no longer describes academic years as a manual workflow dimension.
- Updated README and operator/demo docs so smoke checks and presentations now describe the grade-first structure workflow instead of asking users to maintain visible academic-year records.
- Verification completed for the compatibility pass:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run test:demo-db`
- Verification gap:
  - no live browser smoke was run against the workstation deployment in this turn
- Next recommended action:
  - run a live browser smoke on `/dashboard/structure`, `/dashboard/people`, `/dashboard/inspection`, and `/dashboard/exports`
  - if the school wants a cleanup follow-up later, decide separately whether to retire the internal `AcademicYear` compatibility layer after the grade-first rollout has settled

## 2026-04-23

- Initialized the workflow scaffold.
- Added `AGENTS.md`.
- Added the core `memory-bank` documents.
- Confirmed that the workspace contained no application code at setup time.
- Refined the product direction into a concrete high school school affairs system.
- Updated `prd.md` with version 1 scope, users, data domains, and acceptance examples.
- Updated `tech-stack.md` with a private-deployment full-stack web architecture recommendation.
- Rewrote `implementation-plan.md` into product-specific build phases.
- Scaffolded a `Next.js 16 + TypeScript` application into the repository root.
- Installed and wired `Ant Design`, `next-auth`, `zod`, and `Prisma`.
- Replaced the default landing page with login redirection and added:
  - Bootstrap admin login page
  - protected dashboard shell
  - logout button
  - auth route handler
- Added `.env.example`, `docker-compose.yml`, `prisma.config.ts`, and `prisma/schema.prisma`.
- Generated Prisma Client into `src/generated/prisma`.
- Verification completed:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run db:validate`
  - `npm run db:generate`
- Verification gap:
  - real PostgreSQL connection and migration execution were not verified because no running local database instance was available in this environment
- Captured the user's explicit requirement that class counts per grade, departments, and subjects must be configurable in the system UI.
- Added `tasks/todo.md` and `tasks/lessons.md` for the current Step 3 execution notes.
- Implemented system-admin-only school structure management at `/dashboard/structure`.
- Added server-side actions and validation for:
  - academic year create, edit, delete, and mark-current
  - grade create, rename, and delete
  - class create, rename, and delete under a grade
  - department create, rename, and delete
  - subject create, rename, and delete
- Added `src/lib/prisma.ts` with Prisma 7 PostgreSQL adapter wiring.
- Added `src/lib/authorization.ts` and NextAuth session role propagation for explicit server-side permission checks.
- Installed `@prisma/adapter-pg`, `pg`, and restored `@prisma/client` because the generated Prisma 7 client requires the runtime package.
- Cleaned dashboard and app config copy so the newly exposed structure page is reachable from the sidebar and overview page.
- Verification completed after Step 3:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run db:generate`
  - `npm run db:validate`
- Verification gap:
  - browser-level create/edit/delete testing against a real PostgreSQL database was not completed because no running local database instance is available in this environment
  - `git status` could not be checked because `git` is not available on this machine's PATH
- Started Step 4 and implemented the first teacher/student master-data slice.
- Added `src/modules/people` with:
  - people query normalization and filtered list queries
  - teacher create, update, deactivate, and reactivate server actions
  - student create, update, deactivate, and reactivate server actions
  - a combined people management page for teachers and students
- Added `src/lib/validation/people.ts` for teacher/student form and filter validation.
- Added `requirePeopleManager()` authorization for `SYSTEM_ADMIN` and `DATA_MANAGER`.
- Added `/dashboard/people` and linked it from the sidebar and dashboard overview page.
- People management behavior now supports:
  - teacher filtering by keyword, department, subject, and status
  - student filtering by keyword, grade, class, and status
  - non-destructive stop/start status changes instead of hard delete
  - validation that a selected student class belongs to the selected grade
- Verification completed after Step 4 first slice:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run db:validate`
- Verification gap:
  - browser-level save and filter testing against a real PostgreSQL database was not completed because no running local database instance is available in this environment
  - teacher/student spreadsheet import and filtered export remain pending inside Step 4
- Installed `xlsx` for teacher and student spreadsheet flows.
- Added teacher and student `.xlsx` template download routes.
- Added teacher and student filtered `.xlsx` export routes.
- Added teacher import action:
  - reads the first worksheet
  - matches departments and subjects by name
  - upserts by employee number
  - skips invalid rows and returns a compact error summary
- Added student import action:
  - reads the first worksheet
  - matches academic year, grade, and class by name
  - upserts by student number
  - rejects rows where the selected class does not belong to the selected grade
- Updated `/dashboard/people` with template download, import, and current-filter export controls.
- Verification completed after Step 4 import/export slice:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run db:validate`
- Verification gap:
  - browser-level import/export testing against a real PostgreSQL database was not completed because no running local database instance is available in this environment
  - npm audit now reports `6` vulnerabilities after installing `xlsx` (`5` moderate, `1` high); no forced audit fix was run because it may introduce breaking upgrades
- Next recommended action: start Step 5 by building routine inspection category/item management and inspection record entry.
- Started and completed Step 5 routine inspection management.
- Added `requireInspectionManager()` authorization for `SYSTEM_ADMIN` and `INSPECTION_STAFF`.
- Added `src/lib/validation/inspection.ts` for inspection category, item, record, and filter validation.
- Added `src/modules/inspection` with:
  - inspection record query normalization and filtered list queries
  - category create, rename, and empty-category delete server actions
  - item create, edit, enable, and disable server actions
  - record create and update server actions
  - a combined inspection management page for categories, items, record entry, filtering, and record edits
- Added `/dashboard/inspection` and linked it from the sidebar and dashboard overview page.
- Inspection behavior now supports:
  - configurable inspection categories and items
  - item value types for score, count, and deduction
  - non-destructive item disabling instead of deleting historically used items
  - record entry by date, inspection item, grade, and optional class
  - validation that a selected inspection class belongs to the selected grade
  - record filtering by date range, category, item, grade, and class
- Refreshed dashboard copy and app module highlights to reflect the Step 5 state.
- Verification completed after Step 5:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:validate`
- Verification gap:
  - browser-level inspection create/edit/filter testing against a real PostgreSQL database was not completed because no running local database instance is available in this environment
- Next recommended action: start Step 6 by building statistics views and export center for inspection records.
- Resumed after an interrupted Step 6 run and reread `/memory-bank` plus `tasks/todo.md`.
- Confirmed the current implementation step is Step 6: statistics and export center.
- Found and fixed the interruption gap where `src/app/dashboard/page.tsx` had been deleted before it was recreated.
- Added report/export authorization with `requireReportViewer()` for:
  - `SYSTEM_ADMIN`
  - `DATA_MANAGER`
  - `INSPECTION_STAFF`
  - `READ_ONLY_LEADER`
- Added `src/lib/validation/reporting.ts` for report filter validation.
- Added `src/modules/reporting` with:
  - inspection report filter normalization and summary queries
  - KPI calculation
  - summaries by inspection item, target scope, date, and result type
  - Excel and CSV export builders
  - audit helper for export logging
- Added `/dashboard/exports` with:
  - filter form for date range, category, item, grade, and class
  - KPI cards
  - summary preview tables
  - Excel and CSV export links
  - shortcut back to people export workflows
- Added export routes:
  - `/dashboard/exports/inspection/xlsx`
  - `/dashboard/exports/inspection/csv`
- Enabled the `统计导出` sidebar entry and refreshed the dashboard overview copy.
- Verification completed after Step 6:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:validate`
- Verification gap:
  - browser-level export download testing against a real PostgreSQL database was not completed because no running local database instance is available in this environment
- Next recommended action: start Step 7 stabilization, deployment instructions, and real database smoke testing.
- User asked to simulate with fabricated data because no real database is currently available.
- Checked available database runtimes:
  - Docker/Podman not available
  - `psql` not available
  - no local PostgreSQL Windows service found
  - WSL exists but has no installed Linux distribution
  - `localhost:5432` is not accepting connections
- Added `@electric-sql/pglite` as a development dependency for local Postgres-compatible simulation.
- Added `scripts/simulate-school-affairs.mjs` and `npm run test:demo-db`.
- The simulation script now:
  - creates a temporary PGlite database under `.tmp/pglite-school-affairs`
  - recreates school affairs tables in a Prisma-compatible shape
  - seeds fake data for 4 users, 1 academic year, 3 grades, 12 classes, 6 departments, 6 subjects, 12 teachers, 96 students, 4 inspection categories, 5 inspection items, and 840 inspection records
  - runs summary queries by inspection item, target scope, date, and value type
  - writes one simulated export audit log row
  - writes smoke-test artifacts to `outputs/simulation`
- Generated and verified:
  - `outputs/simulation/inspection-report-simulation.xlsx`
  - `outputs/simulation/inspection-report-simulation.csv`
  - `outputs/simulation/demo-db-summary.json`
- Simulation KPI result:
  - matched records: `840`
  - total value: `45485`
  - average value: `54.15`
  - min/max: `-3` to `100`
- Verification completed after adding the simulation harness:
  - `npm run test:demo-db`
  - Excel workbook readability check through `xlsx`
  - CSV preview check
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:validate`
- Verification gap:
  - this is a local PGlite simulation, not a real Prisma-to-PostgreSQL browser smoke test
- Next recommended action: use this simulation dataset shape to write a real PostgreSQL seed command once PostgreSQL is available, then run browser-level smoke tests.
- Continued Step 7 stabilization and handoff prep.
- Extracted shared deterministic demo data into `scripts/demo-data.mjs`.
- Refactored `scripts/simulate-school-affairs.mjs` to use the shared demo data while keeping the PGlite simulation flow intact.
- Added `scripts/seed-demo-postgres.mjs` for real PostgreSQL demo seeding.
- Added commands:
  - `npm run db:seed:demo`
  - `npm run db:seed:demo:dry-run`
- Updated `README.md` with current implemented modules, database sync, demo seed, and PGlite simulation instructions.
- Added `docs/deployment-and-smoke-test.md` with:
  - environment setup
  - PostgreSQL startup guidance
  - schema sync commands
  - demo seed command
  - browser smoke checklist
  - current known gaps
- Verification completed after seed/docs work:
  - `npm run db:seed:demo:dry-run`
  - `npm run test:demo-db`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:validate`
- Verification gap:
  - `npm run db:seed:demo` was not run because no reachable PostgreSQL instance is available in this environment
  - browser smoke testing still requires a reachable PostgreSQL instance
- Next recommended action: either implement database-backed user management or run the documented browser smoke checklist once PostgreSQL is available.
- Continued Step 7 and implemented database-backed user and permission management.
- Updated authentication so credentials login now:
  - normalizes the username
  - first checks active database users with `passwordHash`
  - verifies passwords with `bcryptjs`
  - falls back to the Bootstrap admin account if no database user can authenticate or the database is unavailable
- Added `src/modules/users` with:
  - system-admin-only user list query
  - user create action
  - display name and role update action
  - account enable/disable action
  - password reset action
  - audit logging for user creation, role changes, status changes, and password resets
- Added `src/lib/validation/users.ts` for username, role, status, and password reset validation.
- Added `/dashboard/users` and linked it from the sidebar and dashboard overview page.
- Updated visible app-shell and module kicker copy so the main UI no longer shows English step labels.
- Updated demo seed data so real PostgreSQL demo users receive hashed passwords and can log in after `npm run db:seed:demo`.
- Updated `README.md` and `docs/deployment-and-smoke-test.md` with database user login, demo accounts, user-management smoke checks, and current gaps.
- Verification completed after database-backed user management:
  - `npm run db:seed:demo:dry-run`
  - `npm run test:demo-db`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:validate`
- Verification gap:
  - `npm run db:seed:demo` was not run because no reachable PostgreSQL instance is available in this environment
  - browser-level login and user-management smoke testing still requires a reachable PostgreSQL instance
- Next recommended action: run the documented browser smoke checklist once a PostgreSQL instance is available, then rehearse backup and restore on the target server.
- User clarified that leadership approval is needed before the school will allow a PostgreSQL environment on the school server.
- Continued Step 7 by preparing leader-facing approval and demo materials.
- Added `/dashboard/showcase` as a Chinese in-app成果展示 page for:
  - current progress
  - 10-minute demo route
  - verified simulation outputs
  - PostgreSQL approval ask
  - suggested closing statement for leadership
- Linked the showcase page from the sidebar and dashboard overview page.
- Added leader demo documents:
  - `docs/leader-demo-package.md`
  - `docs/leader-demo-script.md`
  - `docs/approval-brief.md`
  - `docs/security-and-deployment-plan.md`
- Updated `README.md` with the leader demo package entry points.
- Verification completed for this documentation/showcase slice:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
- Verification gap:
  - this slice is presentation and documentation focused; real browser demonstration still depends on starting the app and, for database-backed live data, having a reachable PostgreSQL instance
- User reported that the leadership demo effect was poor because concrete outcomes could not actually run without PostgreSQL.
- Continued Step 7 by adding a no-database local runnable demo mode.
- Added `src/lib/local-demo-data.ts` with built-in fake users, school structure, teachers, students, inspection items, inspection records, filters, and summary helpers.
- Added `/dashboard/demo` with Chinese sections for:
  - school structure demo
  - user permission demo
  - teacher/student filtering demo
  - routine inspection filtering and summary demo
  - demo Excel/CSV export buttons
- Added demo-only export routes:
  - `/dashboard/demo/exports/inspection/xlsx`
  - `/dashboard/demo/exports/inspection/csv`
- Linked the local demo route from the sidebar, dashboard overview, and showcase page.
- Updated leader demo documents so leadership presentations use `/dashboard/demo` before any PostgreSQL-backed business page.
- Added `docs/local-demo-runbook.md` with exact local startup, login, and demo instructions.
- Verification completed so far:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
- Verification gap:
  - `/dashboard/demo` and demo export routes are verified at build level; full click-through still requires running `npm run dev` in a browser
- User asked for a one-click local demo mode for convenience.
- Added `start-local-demo.cmd` as a Windows double-click launcher.
- Added `scripts/start-local-demo.ps1` to:
  - create or complete `.env.local`
  - install dependencies if `node_modules` is missing
  - open the login page with `callbackUrl=/dashboard/demo`
  - start the Next.js dev server on port `3000`
- Updated the login page and login form so safe `/dashboard...` callback URLs are honored; this lets one-click demo login land directly on `/dashboard/demo`.
- Added `npm run demo:local` as a terminal alias for the same launcher.
- Updated `README.md` and `docs/local-demo-runbook.md` so the recommended path is now double-clicking `start-local-demo.cmd`.
- Verification completed for this launcher slice:
  - PowerShell script parse check
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
- Verification gap:
  - I did not run `start-local-demo.cmd` end-to-end during verification because it intentionally opens a browser and starts a long-running dev server; the script was syntax-checked and the app build includes the callback-enabled login and `/dashboard/demo` route.
- User reported that double-clicking `start-local-demo.cmd` flashed and closed without opening the login page.
- Fixed the launcher diagnostics:
  - `start-local-demo.cmd` now keeps the window open after the PowerShell helper exits.
  - The launcher writes logs to `logs/start-local-demo.log`.
  - The PowerShell helper accepts `-LogFile` and records a transcript when possible.
  - The PowerShell helper accepts `-CheckOnly` for no-browser/no-server validation.
  - Failure messages are now shown in the terminal instead of disappearing.
- Updated `README.md` and `docs/local-demo-runbook.md` with the log location and failure behavior.
- Root cause found:
  - Windows PowerShell 5 parsed the UTF-8 PowerShell script incorrectly because the script contained non-ASCII Chinese strings without a BOM.
  - The PowerShell helper has been converted to ASCII-only console text so `powershell.exe` can parse it reliably.
- Verification completed for this flash-exit fix:
  - PowerShell parse check
  - PowerShell `-CheckOnly`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
- Verification gap:
  - I still did not start the full demo server during automated verification, because it intentionally opens a browser and stays running; the no-browser `-CheckOnly` path now passes under `powershell.exe`.

## 2026-04-24

- User confirmed the local demo now runs, then noticed the demo did not visibly show the implemented people-management Excel import/export capability.
- Continued Step 7 by expanding the no-database local demo people-management evidence.
- Added demo-only people Excel routes that use built-in fake data and do not require PostgreSQL:
  - `/dashboard/demo/people/templates/teachers`
  - `/dashboard/demo/people/templates/students`
  - `/dashboard/demo/people/export/teachers`
  - `/dashboard/demo/people/export/students`
- Updated `/dashboard/demo#people` with:
  - teacher and student import template download buttons
  - teacher and student filtered export buttons
  - a clear explanation that real Excel upload/import and persistent edits require the PostgreSQL-backed `/dashboard/people` page
- Updated `README.md`, `docs/local-demo-runbook.md`, and `docs/leader-demo-script.md` so the leadership demo now explicitly clicks the people Excel template/export actions.
- Added a lesson that leadership demos must expose clickable evidence for each claimed capability.
- Verification completed after this local demo people Excel slice:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
- Verification gap:
  - I did not start the long-running local demo server or click the downloads in a browser during automated verification; the production build confirms all four new demo routes are registered successfully.
- User asked what the project should do next; I recommended moving from “can demo” to “can trial” and then prepared the next handoff materials.
- Continued Step 7 by adding school-server trial deployment documentation:
  - `docs/school-server-pilot-checklist.md`
  - `docs/postgresql-install-and-acceptance-runbook.md`
- Updated `README.md`, `docs/leader-demo-package.md`, and `docs/deployment-and-smoke-test.md` so the new trial handoff docs are discoverable from the main entry points.
- Verification completed for this documentation slice:
  - reviewed document cross-references
  - confirmed new file paths exist
  - confirmed README and handoff docs point to the new materials
- Verification gap:
  - this slice is documentation-only and does not prove a live PostgreSQL installation; real validation still requires a school-approved server or other reachable PostgreSQL instance.

- Resumed after an interrupted Step 7 run by rereading `/memory-bank`, `tasks/todo.md`, and current workspace changes before editing anything else.
- Confirmed the current sub-step is Step 7: apply the approved school trial role model and grade scope.
- Completed the Step 7 trial-role-model slice in code and docs:
  - regenerated Prisma Client after the `UserRole` and `managedGradeId` schema changes
  - kept the new `SCHOOL_LEADER` and `GRADE_MANAGER` roles in auth, session typing, user management, people, inspection, and reporting flows
  - preserved server-side grade-scope enforcement for student import/export, inspection entry, inspection reporting, and page-level visibility
  - updated `memory-bank/prd.md`, `memory-bank/tech-stack.md`, and `memory-bank/implementation-plan.md` to reflect the approved school PostgreSQL pilot and role model
  - rewrote `README.md`, `docs/deployment-and-smoke-test.md`, `docs/postgresql-install-and-acceptance-runbook.md`, `docs/school-server-pilot-checklist.md`, and `docs/security-and-deployment-plan.md` so the handoff docs now match the seeded trial accounts and scope rules
- Found and fixed a real regression in `scripts/simulate-school-affairs.mjs`:
  - `npm run test:demo-db` was failing because the simulated `AuditLog` insert passed `7` parameters into a SQL statement with `6` placeholders
  - removed the stale extra actor parameter so the PGlite simulation works again with the updated trial dataset
- Verification completed for this Step 7 slice:
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:generate`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run db:seed:demo:dry-run`
  - `npm run test:demo-db`
  - `npm run build`
  - `DATABASE_URL=postgresql://school:school@localhost:5432/school_affairs npm run db:validate`
- Verification gap:
  - browser-level smoke testing against a reachable real PostgreSQL instance is still pending
- Next recommended action:
  - run the documented browser smoke checklist with a school-approved PostgreSQL instance, then rehearse backup and restore on the target server

- User narrowed the approved rollout further: start with Grade 11 on the current school-network workstation, keep PostgreSQL on that machine's `D` drive, and let office PCs access only the web application over the LAN.
- Continued Step 7 by landing the workstation-pilot slice in code, scripts, and handoff materials:
  - allowed `SCHOOL_LEADER` sessions to open `/dashboard/inspection` in read-only mode while keeping record entry and configuration actions restricted to editing roles
  - updated `src/lib/local-demo-data.ts` and `scripts/demo-data.mjs` so the seeded pilot accounts now match the approved pack: `admin`, `leader1~leader3`, `grade11.manager1~grade11.manager3`, `data.manager`, and `inspector`
  - added `scripts/start-school-pilot.ps1`, `start-school-pilot.cmd`, and `npm run pilot:school` for Windows workstation deployment with LAN-aware `NEXTAUTH_URL` setup and production-mode startup
  - refreshed `README.md`, leadership demo documents, and the trial deployment runbooks so they now describe the Grade 11 pilot workstation instead of the older generic server-based trial wording
  - refined `memory-bank/prd.md`, `memory-bank/tech-stack.md`, and `memory-bank/implementation-plan.md` so the approved pilot assumptions now match the actual host, network path, and account allocation
- Verification completed for this Step 7 workstation-pilot slice:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run test:demo-db`
  - `npm run db:seed:demo:dry-run`
  - `npm run build`
  - `DATABASE_URL=postgresql://school_admin:school_password@localhost:5432/school_affairs?schema=public npm run db:validate`
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-school-pilot.ps1 -CheckOnly -NoInstall -SkipBuild`
- Verification note:
  - `npm run db:validate` still requires explicitly setting `DATABASE_URL` in the shell for the current Prisma config; `.env.local` alone was not enough in this environment
  - the pilot launcher check passed, but this runtime could not auto-detect a LAN IPv4 address and fell back to `localhost`; the script now warns and supports `-PublicHost` for manual override
- Verification gap:
  - browser-level smoke testing against the real pilot database is still pending because the app has not yet been exercised from a browser after the workstation PostgreSQL install
- Next recommended action:
  - run `npm run pilot:school -- -PublicHost <试点电脑校内IP>`, then execute the documented browser smoke checklist from another office PC on the same school network

- User asked to install PostgreSQL natively on the current workstation `D` drive.
- Continued Step 7 by completing the real workstation database setup:
  - installed native PostgreSQL 17 to `D:\PostgreSQL\17`
  - created the PostgreSQL data directory at `D:\PostgreSQL\data`
  - registered the Windows service `postgresql-xiaowuxitong`
  - created the `school_admin` login and `school_affairs` database on the real local PostgreSQL service
  - updated `.env.local` so the project now points at the real local PostgreSQL instance
  - ran `npm run db:push` successfully against the real local database
- Found and fixed two real PostgreSQL seed-script regressions that the no-database simulation did not expose:
  - `scripts/seed-demo-postgres.mjs` was omitting `createdAt` and `updatedAt` for tables that require `updatedAt`
  - the same script inserted `User` rows before `Grade` rows, which broke the `managedGradeId` foreign key on a real PostgreSQL database
- Verification completed for this workstation database slice:
  - PostgreSQL 17 installer completed successfully
  - `psql` login verified for both `postgres` and `school_admin`
  - `npm run db:push`
  - `npm run db:seed:demo`
  - `npm run test:demo-db`
  - `npm run lint`
  - `npm run typecheck`
  - `netstat -ano | Select-String ':5432'`
- Verification note:
  - the current live PostgreSQL process is running via `pg_ctl` and now listens only on `127.0.0.1:5432` and `[::1]:5432`
  - the Windows service is registered and set to auto-start, but this shell session could not stop or start it directly because service-control permissions returned `Access is denied`
- Verification note:
  - `npm run db:validate` had already passed earlier when `DATABASE_URL` was exported in the shell; after the install, the command still needs either an exported `DATABASE_URL` or a future Prisma config adjustment because `dotenv/config` does not automatically read `.env.local`
- Verification gap:
  - browser-level smoke testing from the pilot workstation browser and from another office PC on the same school network is still pending
  - a reboot-level verification that the registered PostgreSQL Windows service auto-starts cleanly is still pending
- Next recommended action:
  - start the app with `npm run pilot:school -- -PublicHost <试点电脑校内IP>`, then complete the browser smoke checklist and one backup/restore rehearsal on the live workstation database

- User rebooted the pilot workstation and asked me to continue closing out the live Grade 11 pilot checks.
- Continued Step 7 by completing the workstation-side acceptance slice on the real machine:
  - verified after reboot that `postgresql-xiaowuxitong` is `Running`, `Automatic`, and still only listens on `127.0.0.1:5432` and `[::1]:5432`
  - confirmed `school_admin` can still log into `school_affairs` after reboot
  - identified the current school-network IPv4 address as `192.168.1.3`
  - rebuilt the Next.js app and started the production pilot service with `npm run pilot:school -- -PublicHost 192.168.1.3`
  - confirmed the app now listens on `0.0.0.0:3000`, the login page returns `200` through both `http://127.0.0.1:3000/login` and `http://192.168.1.3:3000/login`, and `Test-NetConnection 192.168.1.3 -Port 3000` succeeds locally
  - exercised a real NextAuth login for `leader1` and verified `/dashboard/people`, `/dashboard/exports`, and `/dashboard/inspection` return `200`, while `/dashboard/users` and `/dashboard/structure` redirect to `/dashboard?forbidden=1`
  - exercised a real NextAuth login for `grade11.manager1` and verified `/dashboard/people` and `/dashboard/inspection` return `200`, `/dashboard/users` is forbidden, and the rendered people page contains `高二` but not `高一` or `高三`
  - rehearsed backup and restore on the live workstation database with `pg_dump` and `pg_restore`, restoring into a temporary `school_affairs_restore_smoke` database and validating counts of `users=9`, `students=96`, `teachers=12`, and `inspectionRecords=840`
  - left the latest backup rehearsal artifact at `artifacts/backup-smoke/school_affairs_20260424_200426.dump`
- Verification completed for this post-reboot acceptance slice:
  - `Get-Service -Name postgresql-xiaowuxitong | Select-Object Status,StartType,Name,DisplayName`
  - `netstat -ano | Select-String ':5432'`
  - `D:\PostgreSQL\17\bin\psql.exe -h localhost -U school_admin -d school_affairs -c "SELECT current_database(), current_user;"`
  - `ipconfig`
  - `npm run build`
  - `npm run pilot:school -- -PublicHost 192.168.1.3 -SkipBuild`
  - `netstat -ano | Select-String ':3000'`
  - `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3000/login`
  - `Invoke-WebRequest -UseBasicParsing http://192.168.1.3:3000/login`
  - scripted credential login and page checks for `leader1` and `grade11.manager1`
  - `Test-NetConnection 192.168.1.3 -Port 3000`
  - `D:\PostgreSQL\17\bin\pg_dump.exe`
  - `D:\PostgreSQL\17\bin\pg_restore.exe`
- Verification note:
  - keeping the pilot web service alive for LAN access from this Codex session required launching it outside the sandbox; the production process is now listening on port `3000`
  - backup or restore verification on Windows was more reliable when SQL was piped into `psql` instead of passed through a heavily quoted `-c` argument
- Verification gap:
  - I still cannot click the app in a real browser from a second office PC inside this session, so the final external smoke check on another school-network computer remains pending
  - if another office PC cannot open `http://192.168.1.3:3000/login`, the next check should be Windows Firewall inbound access for TCP `3000` on the Private profile
- Next recommended action:
  - from at least one other office PC on the same school network, open `http://192.168.1.3:3000/login` and run the documented smoke checklist against the live workstation pilot

- User then confirmed from another office PC on the same school network that the pilot site can be opened successfully.
- Closed the last Step 7 blocking validation item in the current pilot scope:
  - external LAN reachability to `http://192.168.1.3:3000` is now confirmed by a second office computer, not only by localhost and self-connect checks on the pilot workstation
  - the current deployment path is therefore verified across rebooted PostgreSQL service startup, local production app startup, local role smoke checks, backup or restore rehearsal, and real same-network client access
- Verification completed for this final pilot-access slice:
  - user-confirmed successful open of the pilot site from another office PC on the same school network
- Verification note:
  - this final confirmation proves the current Windows workstation deployment is reachable over the school LAN; I did not directly observe deeper role-by-role page navigation from that second machine inside this session
- Remaining follow-up:
  - no blocking deployment item remains for the approved Grade 11 pilot; any next work is now optional hardening or feature iteration rather than trial bring-up
- Next recommended action:
  - let the approved Grade 11 users begin the pilot on the live workstation deployment and collect the first round of real operator feedback

- User asked for a consolidated list of all current accounts and passwords plus a detailed Chinese usage guide for the pilot workstation deployment.
- Continued Step 7 handoff documentation by adding `docs/pilot-accounts-and-usage-guide.md`:
  - separated web-login accounts from database maintenance accounts so school operators and technical admins do not confuse them
  - documented the current live web login accounts: `admin`, `leader1~leader3`, `grade11.manager1~grade11.manager3`, `data.manager`, and `inspector`
  - documented the current database maintenance accounts: `school_admin` and `postgres`
  - wrote a detailed Chinese operator guide covering startup, login, role-based responsibilities, page usage, import or export flow, inspection workflow, backup or restore basics, and common troubleshooting
  - explicitly noted that `admin / ChangeMe123!` is currently both the seeded system-admin login and the environment fallback admin credential
- Verification completed for this documentation slice:
  - checked `.env.local` for current bootstrap admin and database credentials
  - checked `scripts/demo-data.mjs` and `scripts/seed-demo-postgres.mjs` for the seeded login account list and default password source
  - queried the live PostgreSQL `User` table to confirm the currently active usernames and roles
  - checked `src/lib/authorization.ts` so the role-by-role Chinese usage notes match the current code-level permission rules
- Verification gap:
  - this slice is documentation-only; I did not click every page again after writing the guide because the role smoke checks had already been completed earlier in the same live environment
- Next recommended action:
  - send the new account guide to the school-side operators, then replace the default passwords after the first onboarding round is complete

- User added four new product requirements:
  - grades need batch increase and decrease for class counts
  - each new academic year should promote active grades automatically and archive the graduating cohort
  - archived graduating students need a dedicated alumni archive center with continued import, export, and edit support
  - each teacher can belong to multiple duty affiliations
- Completed the new Step 3 and Step 4 lifecycle-extension slice in code and live schema:
  - extended `prisma/schema.prisma` with `GradeStage`, grade visibility and graduation metadata, archived-student fields, and teacher `duties`
  - added `src/lib/grade-lifecycle.ts` so grade promotion, alumni naming, and batch class naming use one shared rule set
  - rebuilt school-structure actions and UI so admins can label grades by lifecycle stage, batch-adjust class counts, and start a new academic-year rollover that creates a new high one, promotes active cohorts, archives the old high three as `XXXX届`, and rebinds grade-manager accounts
  - changed people queries so the main `/dashboard/people` page now shows only active front-end students, while archived students are excluded by default
  - added `/dashboard/archive/students` plus archive export and template routes for the alumni archive center
  - updated student create, edit, status, and import flows so active and archived student records are maintained in separate views against the same `Student` table
  - updated teacher create, edit, import, export, and page display to support multiple duties
  - updated the sidebar shell and dashboard layout so users with school-wide archive access can see the new archive module entry
  - refreshed `scripts/demo-data.mjs`, `scripts/seed-demo-postgres.mjs`, and `scripts/simulate-school-affairs.mjs` so the shared demo dataset now includes archived cohorts and teacher duties
  - pushed the live PostgreSQL schema update to the workstation database with `npm run db:push`
- Verification completed for this lifecycle-extension slice:
  - `npm run db:generate`
  - `npm run db:push`
  - `npm run typecheck`
  - `npm run lint`
  - `npm run test:demo-db`
  - `npm run build`
  - `npm run db:seed:demo:dry-run`
  - `DATABASE_URL=postgresql://school_admin:...@localhost:5432/school_affairs?schema=public npm run db:validate`
- Verification gap:
  - I did not run a browser click-through of the newly added archive center and rollover form after this code change inside this session
  - the new academic-year rollover has been verified at type, build, schema, and simulation level, but not yet exercised against the live browser UI with real operator clicks
- Next recommended action:
  - perform one browser smoke pass for `/dashboard/structure`, `/dashboard/people`, and `/dashboard/archive/students`, then decide whether to use the new rollover flow immediately or wait until the next school year boundary

- User then reported that logging into the live workstation URL did not show the claimed new lifecycle changes.
- Investigated the live pilot runtime instead of changing code blindly:
  - confirmed port `3000` was still served by a stale `node` process started at `2026-04-24 20:00:53`
  - confirmed the latest `.next` production build artifacts were newer, with timestamps around `2026-04-25 08:18`
  - concluded the workstation had been rebuilt locally after the lifecycle work, but the school-pilot service had not been restarted, so office PCs were still seeing the older production process
- Corrected the live deployment state:
  - stopped the stale pilot `node` process on port `3000`
  - restarted the school-pilot production service with `scripts/start-school-pilot.ps1 -PublicHost 192.168.1.3 -SkipBuild`
  - confirmed the new live `node` process started at `2026-04-25 08:36:25`
- Completed the previously pending browser-equivalent smoke pass for the new lifecycle flows against the live workstation deployment by scripting real NextAuth credential logins:
  - `admin` can access `/dashboard/structure`, sees `新学年滚动`, and sees `批量调整班级数`
  - `admin` can access `/dashboard/archive/students`
  - `leader1` is still forbidden from `/dashboard/structure`, but can access `/dashboard/archive/students` as intended
  - `grade11.manager1` is forbidden from `/dashboard/structure` and `/dashboard/archive/students`, which matches the current role design
  - `grade11.manager1` can still access `/dashboard/people`, where the archive entry is intentionally absent for that role
- Root-cause clarification for handoff:
  - the user-facing discrepancy was caused by an outdated live process, not by missing code changes
  - some of the newly added UI is role-scoped, so even on the updated build, only system-wide roles see the archive center and only the system administrator sees school-structure rollover controls
- Verification completed for this deployment-correction slice:
  - `netstat -ano | Select-String ':3000'`
  - `Get-Process -Id 11116 | Select-Object Id,ProcessName,Path,StartTime`
  - `Get-ChildItem .next`
  - restart of the school-pilot production process on `192.168.1.3:3000`
  - `Get-Process -Id 20736 | Select-Object Id,ProcessName,Path,StartTime`
  - scripted credential login and page checks for `admin`, `leader1`, and `grade11.manager1`
- Remaining follow-up:
  - no technical blocker remains for these lifecycle changes on the live pilot
  - the only remaining decision is whether to execute the new academic-year rollover now or keep it for the actual school-year transition window

- User then reported a new live-login issue: after closing the browser and reopening the pilot URL, the app still auto-entered the previously logged-in account.
- Investigated the auth flow instead of treating it as a browser quirk:
  - confirmed the login page redirects whenever `getServerSession(authOptions)` still returns a valid `Auth.js` session
  - confirmed the live credentials callback was setting `next-auth.session-token` with an explicit future `Expires` timestamp, so the cookie survived browser restarts
  - checked the upstream `next-auth` runtime and verified the default session `maxAge` is `30` days and the session routes actively reissue cookies with `expires`, so this was expected library behavior rather than a logout-button failure
- Implemented a browser-close re-login fix for the live pilot without replacing the existing `Auth.js` setup:
  - added `src/lib/browser-session.ts` with a dedicated browser-session cookie helper
  - added `getBrowserBoundServerSession()` in `src/lib/auth.ts`
  - changed `/`, `/login`, `/dashboard/layout`, and server-side authorization checks to require both the `Auth.js` session and the browser-session cookie
  - updated the login form to set the browser-session cookie only after a successful sign-in
  - updated the logout button to clear the browser-session cookie explicitly
  - updated the login page so a stale persistent `next-auth` cookie no longer redirects straight into the dashboard; instead it shows the login form again and clears the stale session client-side
- Deployed the fix to the live workstation URL:
  - rebuilt the app successfully
  - restarted the school-pilot production service on `http://192.168.1.3:3000`
- Verification completed for this Step 7 auth-hardening slice:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - live cookie inspection of `/api/auth/callback/credentials`, confirming the old persistent `next-auth.session-token` behavior
  - live workstation restart of the pilot service after the build
  - scripted browser-equivalent verification against `http://192.168.1.3:3000`:
    - with both cookies present, `/dashboard` returns `200`
    - with only the old `next-auth` cookie present and no browser-session cookie, `/dashboard` redirects to `/login`
    - with only the old `next-auth` cookie present and no browser-session cookie, `/login` renders the login form instead of auto-entering the old account
- Remaining follow-up:
  - existing logged-in browsers will need one fresh re-login after this change because the new browser-session cookie only appears after the next successful login
  - the academic-year rollover timing decision is still separate and still pending

- User then rebooted the pilot workstation and reported that `http://192.168.1.3:3000/login` could no longer be reached.
- Investigated the reboot recovery state before changing anything:
  - confirmed PostgreSQL was still healthy and auto-started as expected through the `postgresql-xiaowuxitong` Windows service
  - confirmed nothing was listening on TCP `3000`, so the outage was at the web-app layer rather than the database layer
  - confirmed `.env.local` had been corrupted to `NEXTAUTH_URL=http://1:3000`
  - traced that bad value to the pilot launcher: when only one LAN IP candidate existed, the script sometimes treated it as a scalar string and then used `[0]`, which produced only the first character `1`
- Fixed the workstation pilot launcher:
  - updated `scripts/start-school-pilot.ps1` so LAN address detection only accepts real private IPv4 addresses
  - added an `ipconfig` fallback for environments where `Get-NetIPAddress` is incomplete or unavailable
  - fixed the single-candidate handling so a lone value like `192.168.1.3` stays intact instead of collapsing to `1`
- Restored the live workstation deployment:
  - reran the launcher check successfully and verified it now resolves the pilot URL as `http://192.168.1.3:3000`
  - relaunched the production school-pilot web service on `192.168.1.3:3000`
  - confirmed `.env.local` is back to `NEXTAUTH_URL=http://192.168.1.3:3000`
  - confirmed the new live `node` process is listening on `0.0.0.0:3000`
  - confirmed `/login` returns `200` through both `http://127.0.0.1:3000/login` and `http://192.168.1.3:3000/login`
- Verification completed for this reboot-recovery slice:
  - `netstat -ano | Select-String ':3000'`
  - `Get-Service -Name postgresql-xiaowuxitong | Select-Object Status,StartType,Name,DisplayName`
  - `ipconfig`
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-school-pilot.ps1 -CheckOnly -NoInstall -SkipBuild`
  - relaunch of the live school-pilot production process on `192.168.1.3:3000`
  - `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3000/login`
  - `Invoke-WebRequest -UseBasicParsing http://192.168.1.3:3000/login`
- Remaining follow-up:
  - the site is back online now, but the web app still does not auto-start after a Windows reboot because it is launched by the pilot script rather than registered as a Windows service or scheduled task
  - if the school wants “reboot后站点自动恢复”, the next hardening step is to register the web app for auto-start with an explicit Windows-level startup mechanism

- User explicitly approved the next hardening step to reduce reboot-time manual work.
- Attempted a system-level Windows Scheduled Task first:
  - tried to register `SchoolAffairsPilotAutoStart` as an `AtStartup` task that would run the pilot launcher under `SYSTEM`
  - Windows returned `Access is denied`, so the current account does not have enough rights on this machine to create a system-level startup task
- Switched to a user-logon auto-start fallback that works under the current permissions:
  - added `scripts/autostart-school-pilot.cmd`
  - placed that launcher in `C:\Users\94384\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\autostart-school-pilot.cmd`
  - kept the launcher minimal so it starts `start-school-pilot.ps1` in the background with `-NoInstall -SkipBuild`
- Tested the real startup artifact instead of trusting file placement alone:
  - the first `vbs`-based startup attempt did not reliably bring the site back, so that unused helper was removed from the repo
  - ran the final Startup-folder `cmd` launcher directly
  - confirmed it restored the site and brought `127.0.0.1:3000/login` and `192.168.1.3:3000/login` back to `200`
- Verification completed for this auto-start hardening slice:
  - failed scheduled-task registration attempt captured with `Access is denied`
  - confirmed Startup-folder launcher file exists at the current Windows user's Startup path
  - real execution test of `autostart-school-pilot.cmd`
  - `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3000/login`
  - `Invoke-WebRequest -UseBasicParsing http://192.168.1.3:3000/login`
- Current state clarification:
  - after a reboot, once this Windows account logs in, the web app should now auto-start without manually rerunning `start-school-pilot.cmd`
  - truly pre-login startup still requires administrator-level Windows configuration that is not available from the current account

## Entry Template

### YYYY-MM-DD HH:MM

- Objective:
- Changes made:
- Verification:
- Risks or blockers:
- Next step:

## 2026-04-26

- User clarified and approved the next grade-model redesign with concrete examples:
  - active cohorts should be labeled by enrollment year such as `2025级`
  - when `2025级` is created, the active front-end should show `2023级`, `2024级`, and `2025级`
  - older cohorts such as `2022级` should move automatically into the alumni archive and display as `2022级入学 / 2025届毕业`
- Completed a repository-wide impact review instead of changing code blindly:
  - checked the Prisma schema, grade lifecycle helper, school-structure actions and page, people actions and queries, archive page, reporting and inspection filters, user scope queries, spreadsheet templates, and demo or seed data
  - confirmed that the current implementation still hard-codes `HIGH_ONE`, `HIGH_TWO`, `HIGH_THREE`, and `ALUMNI` assumptions across database schema, rollover logic, imports, filters, exports, and demo data
- Updated source-of-truth docs so future implementation follows the approved enrollment-year model instead of the old stage-based model:
  - updated `prd.md`
  - updated `implementation-plan.md`
  - updated `architecture.md`
- Verification completed for this analysis and planning slice:
  - reread all required `memory-bank` files before analysis
  - searched the codebase for stage-based and archive-related assumptions with ripgrep
  - manually reviewed the main affected modules before documenting the redesign boundary
- Verification gap:
  - no runtime code has been changed yet for this redesign
  - browser behavior, database schema migration, and live workstation deployment remain on the old stage-based model until the implementation slice is executed
- Risks or blockers:
  - the redesign touches shared school-structure, people, archive, import or export, and reporting paths, so schema migration and old data backfill need to be done carefully
  - current Grade 11-scoped pilot accounts will need to be rebound to enrollment-year cohorts during implementation
- Next step:
  - implement the Step 3 and Step 4 enrollment-year refactor in small verified slices: schema plus lifecycle helpers first, then structure UI and rollover logic, then people or archive queries and templates, then seed/demo data and smoke testing

### 2026-04-26 22:12

- Objective:
  - implement the approved enrollment-year cohort model so the system uses `2023级 / 2024级 / 2025级` active cohorts and archives older cohorts as labels like `2022级入学 / 2025届毕业`
- Changes made:
  - extended `Grade` with `enrollmentYear`, uniqueness, and indexing in `prisma/schema.prisma`
  - replaced the shared grade lifecycle helper with enrollment-year naming, parsing, sorting, and rollover support in `src/lib/grade-lifecycle.ts`
  - refactored school-structure validation, admin actions, and the structure page so grades are created and edited by enrollment year and academic-year rollover now keeps only the latest three cohorts active
  - updated people, archive, inspection, reporting, and user-scope queries to order and filter by enrollment-year active/archive rules instead of stage labels
  - updated active and archived student import templates plus local demo student templates to use cohort labels like `2025级` and `2022级入学 / 2025届毕业`
  - updated demo dataset, PostgreSQL demo seed, PGlite simulation, and local demo data so the new cohort model remains consistent across verification paths
  - refreshed user-facing copy in the archive center, dashboard shell, and shared app highlights so the visible product language matches the new cohort model
- Verification:
  - regenerated Prisma client against the updated schema with `npm run db:generate` after loading `DATABASE_URL` from `.env.local`
  - `npm run typecheck`
  - `npm run lint`
  - `npm run build`
  - `npm run test:demo-db`
  - `npm run db:seed:demo:dry-run`
  - `npm run db:validate`
  - restarted the live school-pilot service on the workstation so the LAN deployment picks up the new build
  - revalidated the live login URL at `http://192.168.1.3:3000/login` after the restart
- Risks or blockers:
  - operator handoff docs outside `memory-bank` still contain older Grade 11 pilot wording and have not been rewritten to explain the new enrollment-year structure end-to-end
- Next step:
  - run one browser smoke pass on `/dashboard/structure`, `/dashboard/people`, and `/dashboard/archive/students` from the live LAN deployment, then update operator-facing docs if the school wants the new cohort language everywhere
